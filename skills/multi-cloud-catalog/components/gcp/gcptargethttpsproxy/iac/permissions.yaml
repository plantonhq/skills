# Least-privilege permissions the IaC runner's GCP principal needs to
# provision, update, and destroy this component with the official modules.
# Every entry is `derived` (static analysis of the module sources, cited in
# notes) until the live capture lane upgrades it to `proven`. Schema:
# iac/componentpermissions/v1.
apiVersion: iac.planton.dev/v1
kind: ComponentPermissions
metadata:
  name: gcptargethttpsproxy
spec:
  gcp:
    groups:
      - purpose: EnableComputeApi
        permissions:
          - serviceusage.services.enable
          - serviceusage.services.get
        provenance: derived
        notes: Covers google_project_service enabling compute.googleapis.com on the target project. disable_on_destroy is false in the module, so the disable permission is never needed.
      - purpose: ManageTargetHttpsProxy
        permissions:
          - compute.targetHttpsProxies.create
          - compute.targetHttpsProxies.get
          - compute.targetHttpsProxies.delete
          - compute.targetHttpsProxies.update
          - compute.targetHttpsProxies.setUrlMap
          - compute.targetHttpsProxies.setSslCertificates
          - compute.targetHttpsProxies.setSslPolicy
          - compute.targetHttpsProxies.setQuicOverride
          - compute.targetHttpsProxies.setCertificateMap
          - compute.globalOperations.get
        provenance: derived
        notes: Covers google_compute_target_https_proxy -- create, read/refresh, destroy, and polling the global operation every mutation returns. The provider mutates each in-place field through its dedicated RPC (setUrlMap, setSslCertificates, setSslPolicy, setQuicOverride, setCertificateMap), while server_tls_policy and certificate_manager_certificates changes PATCH the proxy, which authorizes as update.
      - purpose: UseReferencedFrontendResources
        permissions:
          - compute.urlMaps.use
          - compute.sslCertificates.get
          - compute.sslPolicies.use
          - certificatemanager.certmaps.use
          - networksecurity.serverTlsPolicies.use
        provenance: derived
        notes: "Service-side checks, not module API calls -- GCP's targetHttpsProxies.insert reference documents each: compute.urlMaps.use on the referenced urlMap (always, since url_map is required), compute.sslCertificates.get on each attached certificate (GCP defines NO compute.sslCertificates.use -- attaching authorizes with GET, verified against the testable-permissions inventory), compute.sslPolicies.use when ssl_policy is set, certificatemanager.certmaps.use when certificate_map is set, and networksecurity.serverTlsPolicies.use when server_tls_policy is set. Only urlMaps.use is unconditional; grant the others per the certificate/TLS mechanism the spec actually uses."
