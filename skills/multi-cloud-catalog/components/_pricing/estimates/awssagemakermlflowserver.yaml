# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awssagemakermlflowserver
spec:
  presets:
    - preset: 01-team-experiment-tracking
      region_assumption: us-west-2
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon SageMaker
          sku_meter: MLflow tracking server hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one tracking server x 730 hours at its size's hourly rate (Small assumed when the manifest omits size -- the AWS default) -- the meter runs from Created onward with no stop control
          list_unit_price: "0.6"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonSageMaker/20260815025356/us-west-2/index.json
          retrieved_on: "2026-08-16"
          list_cost: "438.0000"
      total_list_cost: "438.0000"
      exclusions:
        - experiment metadata stored ON the tracking server (runs, params, metrics) bills $0.10 per GB-month as tracking activity accumulates -- workload-driven, near zero at creation, invisible to this manifest
        - MLflow artifacts (model files, run outputs) bill S3 storage and requests at the artifact_store_uri location, on the bucket's own component
        - data transfer out of the region follows client traffic
      notes: Estimated at the full month -- dedicated hourly capacity with no pause; deleting the server is the only off switch, and the artifacts survive deletion in the customer's S3 bucket.
    - preset: 02-regulated-experiment-tracking
      region_assumption: us-west-2
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon SageMaker
          sku_meter: MLflow tracking server hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one tracking server x 730 hours at its size's hourly rate (Small assumed when the manifest omits size -- the AWS default) -- the meter runs from Created onward with no stop control
          list_unit_price: "1.04"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonSageMaker/20260815025356/us-west-2/index.json
          retrieved_on: "2026-08-16"
          list_cost: "759.2000"
      total_list_cost: "759.2000"
      exclusions:
        - experiment metadata stored ON the tracking server (runs, params, metrics) bills $0.10 per GB-month as tracking activity accumulates -- workload-driven, near zero at creation, invisible to this manifest
        - MLflow artifacts (model files, run outputs) bill S3 storage and requests at the artifact_store_uri location, on the bucket's own component
        - models auto-registered into the SageMaker Model Registry add no registry charge -- the registry is free
        - data transfer out of the region follows client traffic
      notes: Estimated at the full month -- dedicated hourly capacity with no pause; deleting the server is the only off switch, and the artifacts survive deletion in the customer's S3 bucket.
