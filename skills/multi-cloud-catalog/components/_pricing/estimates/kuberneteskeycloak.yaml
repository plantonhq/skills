# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its capacity derivation
# (../capacity/, the machine-executable value-to-capacity rules): each
# preset states the CPU/memory requests and limits and the persistent
# volume storage it reserves from its target cluster, with a basis sentence
# tracing every total back to the manifest's values. Cluster-capacity
# components carry no priced lines -- the dollar value of the reservation
# is the target cluster's node and storage-class economics, which this
# catalog cannot know. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: kuberneteskeycloak
spec:
  presets:
    - preset: 01-standard
      capacity_footprint:
        cpu_requests: 500m
        memory_requests: 1536Mi
        cpu_limits: "2"
        memory_limits: 3Gi
        basis: 2 instances x (250m CPU / 768Mi memory requests, 1 CPU / 1536Mi limits)
      exclusions:
        - the dollar value of this capacity is the target cluster's node economics
        - the database holding every realm and user carries its own footprint on its own resource
        - the Keycloak Operator reconciling this resource carries its own footprint on KubernetesKeycloakOperator
    - preset: 02-dev-sandbox
      capacity_footprint:
        cpu_requests: 250m
        memory_requests: 768Mi
        cpu_limits: "1"
        memory_limits: 1Gi
        basis: 1 instance x (250m CPU / 768Mi memory requests, 1000m CPU / 1Gi limits) from the spec-declared defaults (the modules apply them when the manifest omits resources)
      exclusions:
        - the dollar value of this capacity is the target cluster's node economics
        - the dev-mem sandbox database lives in the pod's own memory -- no storage is reserved and every realm and user is lost on restart
        - the Keycloak Operator reconciling this resource carries its own footprint on KubernetesKeycloakOperator
