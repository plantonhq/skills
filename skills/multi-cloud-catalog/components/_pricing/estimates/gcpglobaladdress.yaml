# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: gcpglobaladdress
spec:
  presets:
    - preset: 01-external-static-ip
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Compute Engine
          sku_meter: reserved external IPv4
          pricing_unit: IP-hours
          pricing_quantity: "730"
          quantity_basis: one static external IPv4 x 730 hours at the reserved-but-unattached rate -- the reservation's committed charge until a global forwarding rule takes it, and its worst case
          list_unit_price: "0.01"
          price_source: https://cloudbilling.googleapis.com/v1/services/6F81-5844-456A/skus
          retrieved_on: "2026-08-16"
          list_cost: "7.3000"
      total_list_cost: "7.3000"
      exclusions:
        - the attached state -- assigned to a global forwarding rule the IP is free; the forwarding rule and the load balancer behind it bill on their own resources, outside this manifest
      notes: Priced at the reserved-idle rate ($0.01/hr) because that is the only state the manifest itself commits to. A global address exists to front a global load balancer, and in that attached state Google charges nothing for it -- the estimate is the cost of NOT finishing the wiring.
    - preset: 02-internal-vpc-peering-range
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - whatever fills the private range bills elsewhere -- managed-service instances in a VPC_PEERING range and the services behind a PRIVATE_SERVICE_CONNECT endpoint are their own components
      notes: An INTERNAL global reservation (VPC peering range, PSC endpoint address) never bills, however wide the prefix -- this estimate is genuinely zero. Priced at the reserved-idle rate ($0.01/hr) because that is the only state the manifest itself commits to. A global address exists to front a global load balancer, and in that attached state Google charges nothing for it -- the estimate is the cost of NOT finishing the wiring.
    - preset: 03-private-service-connect
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - whatever fills the private range bills elsewhere -- managed-service instances in a VPC_PEERING range and the services behind a PRIVATE_SERVICE_CONNECT endpoint are their own components
      notes: An INTERNAL global reservation (VPC peering range, PSC endpoint address) never bills, however wide the prefix -- this estimate is genuinely zero. Priced at the reserved-idle rate ($0.01/hr) because that is the only state the manifest itself commits to. A global address exists to front a global load balancer, and in that attached state Google charges nothing for it -- the estimate is the cost of NOT finishing the wiring.
