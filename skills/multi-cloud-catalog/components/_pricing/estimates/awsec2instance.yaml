# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awsec2instance
spec:
  presets:
    - preset: 01-ssm-managed
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Elastic Compute Cloud
          sku_meter: instance hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one On-Demand instance billed every hour of the month at the shared-tenancy Linux/UNIX rate -- the manifest does not declare the AMI's platform, so Linux/UNIX is the stated assumption
          list_unit_price: "0.0168"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonEC2/20260814185001/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "12.2640"
        - service_name: Amazon Elastic Block Store
          sku_meter: EBS storage
          pricing_unit: GB-months
          pricing_quantity: "30"
          quantity_basis: the reshaped root volume's size (root_block_device.volume_size_gb) at the gp3 rate, billing from creation full or empty
          list_unit_price: "0.08"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonEC2/20260814011739/us-east-1/index.json
          retrieved_on: "2026-08-14"
          list_cost: "2.4000"
      total_list_cost: "14.6640"
      exclusions:
        - a subnet that maps public IPs on launch adds the hourly public-IPv4 charge without this manifest saying so
        - data transfer between AZs and out of the region follows the workload's traffic
        - EBS snapshots taken of the volumes bill per GB-month outside this component
      notes: Priced at the shared-tenancy Linux/UNIX On-Demand rate -- a Windows or commercial-Linux AMI bills license-included rates above this estimate. The estimate assumes the instance runs all 730 hours; stopping it stops the instance-hours meter but keeps every EBS volume billing.
    - preset: 02-launch-template
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Elastic Compute Cloud
          sku_meter: instance hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one On-Demand instance billed every hour of the month at the shared-tenancy Linux/UNIX rate -- the manifest does not declare the AMI's platform, so Linux/UNIX is the stated assumption
          list_unit_price: "0.0336"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonEC2/20260814185001/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "24.5280"
      total_list_cost: "24.5280"
      exclusions:
        - the root volume's size comes from the AMI or launch template's block-device mapping, invisible in this manifest -- its GB-month charge is excluded
        - a subnet that maps public IPs on launch adds the hourly public-IPv4 charge without this manifest saying so
        - data transfer between AZs and out of the region follows the workload's traffic
        - EBS snapshots taken of the volumes bill per GB-month outside this component
      notes: Priced at the shared-tenancy Linux/UNIX On-Demand rate -- a Windows or commercial-Linux AMI bills license-included rates above this estimate. The estimate assumes the instance runs all 730 hours; stopping it stops the instance-hours meter but keeps every EBS volume billing.
    - preset: 03-spot-worker
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Elastic Block Store
          sku_meter: EBS storage
          pricing_unit: GB-months
          pricing_quantity: "50"
          quantity_basis: the reshaped root volume's size (root_block_device.volume_size_gb) at the gp3 rate, billing from creation full or empty
          list_unit_price: "0.08"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonEC2/20260814011739/us-east-1/index.json
          retrieved_on: "2026-08-14"
          list_cost: "4.0000"
      total_list_cost: "4.0000"
      exclusions:
        - running hours bill the fluctuating Spot market rate, typically 60-90% below On-Demand and never above it -- no committed instance rate exists
        - Spot capacity can be reclaimed with two minutes' notice -- the discount's price
        - a subnet that maps public IPs on launch adds the hourly public-IPv4 charge without this manifest saying so
        - data transfer between AZs and out of the region follows the workload's traffic
        - EBS snapshots taken of the volumes bill per GB-month outside this component
      notes: Spot has no committed list price -- the market sets the hourly rate under the published On-Demand ceiling, which is why this estimate carries no instance-hours line. The estimate assumes the instance runs all 730 hours; stopping it stops the instance-hours meter but keeps every EBS volume billing.
    - preset: 04-static-eni-identity
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Elastic Compute Cloud
          sku_meter: instance hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one On-Demand instance billed every hour of the month at the shared-tenancy Linux/UNIX rate -- the manifest does not declare the AMI's platform, so Linux/UNIX is the stated assumption
          list_unit_price: "0.085"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonEC2/20260814185001/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "62.0500"
        - service_name: Amazon Elastic Block Store
          sku_meter: EBS storage
          pricing_unit: GB-months
          pricing_quantity: "40"
          quantity_basis: the reshaped root volume's size (root_block_device.volume_size_gb) at the gp3 rate, billing from creation full or empty
          list_unit_price: "0.08"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonEC2/20260814011739/us-east-1/index.json
          retrieved_on: "2026-08-14"
          list_cost: "3.2000"
      total_list_cost: "65.2500"
      exclusions:
        - a subnet that maps public IPs on launch adds the hourly public-IPv4 charge without this manifest saying so
        - data transfer between AZs and out of the region follows the workload's traffic
        - EBS snapshots taken of the volumes bill per GB-month outside this component
      notes: Priced at the shared-tenancy Linux/UNIX On-Demand rate -- a Windows or commercial-Linux AMI bills license-included rates above this estimate. The estimate assumes the instance runs all 730 hours; stopping it stops the instance-hours meter but keeps every EBS volume billing.
    - preset: 05-capacity-block-ml
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Elastic Block Store
          sku_meter: EBS storage
          pricing_unit: GB-months
          pricing_quantity: "500"
          quantity_basis: the reshaped root volume's size (root_block_device.volume_size_gb) at the gp3 rate, billing from creation full or empty
          list_unit_price: "0.08"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonEC2/20260814011739/us-east-1/index.json
          retrieved_on: "2026-08-14"
          list_cost: "40.0000"
        - service_name: Amazon Elastic Block Store
          sku_meter: provisioned throughput
          pricing_unit: MiBps-months
          pricing_quantity: "875"
          quantity_basis: gp3 includes a 125-MiB/s baseline free -- only the excess over it (root_block_device.throughput_mibps minus 125) bills monthly
          list_unit_price: "0.04"
          price_source: https://aws.amazon.com/ebs/pricing/
          retrieved_on: "2026-08-19"
          list_cost: "35.0000"
      total_list_cost: "75.0000"
      exclusions:
        - the ML Capacity Block was purchased up front at its own committed price and bills whether or not this instance runs -- the instance adds no hourly compute charge here
        - a subnet that maps public IPs on launch adds the hourly public-IPv4 charge without this manifest saying so
        - data transfer between AZs and out of the region follows the workload's traffic
        - EBS snapshots taken of the volumes bill per GB-month outside this component
      notes: Only the EBS volumes bill through this manifest -- the reserved GPU capacity was bought as the Capacity Block itself. The estimate assumes the instance runs all 730 hours; stopping it stops the instance-hours meter but keeps every EBS volume billing.
