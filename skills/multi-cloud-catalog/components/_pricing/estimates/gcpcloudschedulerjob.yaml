# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: gcpcloudschedulerjob
spec:
  presets:
    - preset: 01-basic-http-job
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Cloud Scheduler
          sku_meter: job days
          pricing_unit: job-days
          pricing_quantity: "31"
          quantity_basis: one job existing every day of the month -- 31 job-days, the SKU's own displayed monthly quantity (31 x the per-job-day rate is Google's published $0.10 per job per month) -- billed regardless of how many times the schedule fires
          list_unit_price: "0.003225806"
          price_source: https://cloudbilling.googleapis.com/v1/services/1F14-4801-0E16/skus
          retrieved_on: "2026-08-16"
          list_cost: "0.099999986"
      total_list_cost: "0.099999986"
      exclusions:
        - "dispatch volume is free -- the meter counts job-days, not executions -- but whatever each dispatch invokes bills on its own component: Cloud Run or Cloud Functions compute behind an HTTP target, Pub/Sub message charges behind a topic target"
        - network egress of dispatch requests follows target placement
      notes: The first 93 job-days per billing account per month are free -- exactly three always-on jobs (3 jobs x 31 days) -- so an account's first three jobs pay nothing and this may be one of them. List total shown without the free allotment, which is account-wide and not visible from any single manifest.
    - preset: 02-pubsub-publisher
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Cloud Scheduler
          sku_meter: job days
          pricing_unit: job-days
          pricing_quantity: "31"
          quantity_basis: one job existing every day of the month -- 31 job-days, the SKU's own displayed monthly quantity (31 x the per-job-day rate is Google's published $0.10 per job per month) -- billed regardless of how many times the schedule fires
          list_unit_price: "0.003225806"
          price_source: https://cloudbilling.googleapis.com/v1/services/1F14-4801-0E16/skus
          retrieved_on: "2026-08-16"
          list_cost: "0.099999986"
      total_list_cost: "0.099999986"
      exclusions:
        - "dispatch volume is free -- the meter counts job-days, not executions -- but whatever each dispatch invokes bills on its own component: Cloud Run or Cloud Functions compute behind an HTTP target, Pub/Sub message charges behind a topic target"
        - network egress of dispatch requests follows target placement
      notes: The first 93 job-days per billing account per month are free -- exactly three always-on jobs (3 jobs x 31 days) -- so an account's first three jobs pay nothing and this may be one of them. List total shown without the free allotment, which is account-wide and not visible from any single manifest.
    - preset: 03-secure-cloud-run-trigger
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Cloud Scheduler
          sku_meter: job days
          pricing_unit: job-days
          pricing_quantity: "31"
          quantity_basis: one job existing every day of the month -- 31 job-days, the SKU's own displayed monthly quantity (31 x the per-job-day rate is Google's published $0.10 per job per month) -- billed regardless of how many times the schedule fires
          list_unit_price: "0.003225806"
          price_source: https://cloudbilling.googleapis.com/v1/services/1F14-4801-0E16/skus
          retrieved_on: "2026-08-16"
          list_cost: "0.099999986"
      total_list_cost: "0.099999986"
      exclusions:
        - "dispatch volume is free -- the meter counts job-days, not executions -- but whatever each dispatch invokes bills on its own component: Cloud Run or Cloud Functions compute behind an HTTP target, Pub/Sub message charges behind a topic target"
        - network egress of dispatch requests follows target placement
      notes: The first 93 job-days per billing account per month are free -- exactly three always-on jobs (3 jobs x 31 days) -- so an account's first three jobs pay nothing and this may be one of them. List total shown without the free allotment, which is account-wide and not visible from any single manifest.
