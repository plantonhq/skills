# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awsclientvpn
spec:
  presets:
    - preset: 01-certificate-split-tunnel
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Virtual Private Cloud
          sku_meter: endpoint association hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one hourly endpoint association per associated subnet x 730 hours, billed whether or not anyone is connected
          list_unit_price: "0.1"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonVPC/20260724154225/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "73.0000"
      total_list_cost: "73.0000"
      exclusions:
        - client connection hours bill per concurrently connected client every hour a session stays up -- entirely workforce-dependent (session_timeout_hours and disconnect_on_session_timeout are the levers on lingering sessions)
        - data transfer out of the VPC (internet, cross-AZ, on-premises) bills at the network components that carry it
        - connection-log ingestion and storage bill to the destination CloudWatch log group, not the endpoint
      notes: The association hours are the committed floor; every other dollar follows connected clients, and no manifest can know how many people connect.
    - preset: 02-certificate-full-tunnel
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Virtual Private Cloud
          sku_meter: endpoint association hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one hourly endpoint association per associated subnet x 730 hours, billed whether or not anyone is connected
          list_unit_price: "0.1"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonVPC/20260724154225/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "73.0000"
      total_list_cost: "73.0000"
      exclusions:
        - client connection hours bill per concurrently connected client every hour a session stays up -- entirely workforce-dependent (session_timeout_hours and disconnect_on_session_timeout are the levers on lingering sessions)
        - data transfer out of the VPC (internet, cross-AZ, on-premises) bills at the network components that carry it
        - full tunnel routes ALL client internet traffic into the VPC, so egress rides the NAT gateway's data-processing and data-transfer meters on the NAT-ed subnet's own components
        - connection-log ingestion and storage bill to the destination CloudWatch log group, not the endpoint
      notes: The association hours are the committed floor; every other dollar follows connected clients, and no manifest can know how many people connect.
    - preset: 03-saml-sso
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Virtual Private Cloud
          sku_meter: endpoint association hours
          pricing_unit: hours
          pricing_quantity: "1460"
          quantity_basis: one hourly endpoint association per associated subnet x 730 hours, billed whether or not anyone is connected
          list_unit_price: "0.1"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonVPC/20260724154225/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "146.0000"
      total_list_cost: "146.0000"
      exclusions:
        - client connection hours bill per concurrently connected client every hour a session stays up -- entirely workforce-dependent (session_timeout_hours and disconnect_on_session_timeout are the levers on lingering sessions)
        - data transfer out of the VPC (internet, cross-AZ, on-premises) bills at the network components that carry it
        - connection-log ingestion and storage bill to the destination CloudWatch log group, not the endpoint
      notes: The association hours are the committed floor; every other dollar follows connected clients, and no manifest can know how many people connect.
