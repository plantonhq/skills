# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file joins the component's estimate model (../models/, the authored
# quantities with their derivations) with the provider's price book
# (../pricebook/, pinned unit prices with source URL and retrieval date):
# every line cost and preset total is computed exactly, lines are ordered
# largest cost first, and the catalog-data conformance gate re-verifies all
# arithmetic. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: gcpspannerdatabase
spec:
  presets:
    - preset: 01-basic-database
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - an empty database stores almost nothing -- database storage accrues per GiB-month at the parent instance configuration's rate (regional SSD includes all three replicas) as applications write
        - the 24h PITR window retains overwritten versions as billed storage, in proportion to the write rate
        - compute capacity bills hourly on the GcpSpannerInstance, idle or busy -- the always-on cost of Spanner lives there
      notes: Creation is free; the estimate is zero because the manifest commits no bytes and the database rides an instance whose compute is billed on its own component.
    - preset: 02-postgresql-database
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - stored data bills per GiB-month at the parent instance configuration's rate; the dialect changes syntax, not the rate card
        - the 7d PITR window (the maximum) keeps every overwritten version billing for a week -- the costliest retention choice on write-heavy databases, per Google's own PITR guidance
        - compute capacity bills on the GcpSpannerInstance
      notes: Zero committed at creation -- the maximum PITR window is armed by the manifest but bills nothing until writes create versions to retain.
    - preset: 03-cmek-encrypted
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - stored data bills per GiB-month at the parent instance configuration's rate; the 3d PITR window retains overwritten versions as billed storage
        - the CMEK key's version storage and per-operation charges bill on the referenced GcpKmsKey component
        - compute capacity bills on the GcpSpannerInstance; drop protection changes deletability, not price
      notes: Zero committed at creation -- CMEK and drop protection are posture choices with no meter of their own on this resource.
