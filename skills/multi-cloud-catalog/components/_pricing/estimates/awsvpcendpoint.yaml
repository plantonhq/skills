# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awsvpcendpoint
spec:
  presets:
    - preset: 01-s3-gateway
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - the S3 or DynamoDB traffic riding this endpoint bills requests and storage on those services' own components, and the zero here is the endpoint's absent meter, not the workload's spend
      notes: A Gateway endpoint is a route-table entry with no meter of any kind -- the committed monthly cost is honestly zero, and it is often a net saver because S3/DynamoDB traffic riding it stops paying NAT gateway data-processing rates.
    - preset: 02-interface-endpoint
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Virtual Private Cloud
          sku_meter: VPC endpoint hours
          pricing_unit: hours
          pricing_quantity: "1460"
          quantity_basis: one ENI per subnet, each billing its own endpoint-hour in its Availability Zone every hour of the month (AWS allows one endpoint subnet per AZ, so the subnet count is the AZ count); every catalog preset leaves region a placeholder, so this prices the us-east-1 rate
          list_unit_price: "0.01"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonVPC/20260724154225/us-east-1/index.json
          retrieved_on: "2026-08-18"
          list_cost: "14.6000"
      total_list_cost: "14.6000"
      exclusions:
        - data processing bills per GB through the endpoint regardless of the traffic's source or destination, on a regional tier that aggregates across ALL interface endpoints in the region
        - some AWS services fold their interface-endpoint cost into their own service pricing, and the PrivateLink service being reached bills its owner separately (endpoint-service hours, cross-region active-region hours)
      notes: The hourly meter survives even a rejected or failed service association -- AWS bills while the endpoint remains provisioned, and a rejected endpoint must be DELETED to stop the meter, not just left unassociated.
    - preset: 03-privatelink-service
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Virtual Private Cloud
          sku_meter: VPC endpoint hours
          pricing_unit: hours
          pricing_quantity: "1460"
          quantity_basis: one ENI per subnet, each billing its own endpoint-hour in its Availability Zone every hour of the month (AWS allows one endpoint subnet per AZ, so the subnet count is the AZ count); every catalog preset leaves region a placeholder, so this prices the us-east-1 rate
          list_unit_price: "0.01"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonVPC/20260724154225/us-east-1/index.json
          retrieved_on: "2026-08-18"
          list_cost: "14.6000"
      total_list_cost: "14.6000"
      exclusions:
        - data processing bills per GB through the endpoint regardless of the traffic's source or destination, on a regional tier that aggregates across ALL interface endpoints in the region
        - some AWS services fold their interface-endpoint cost into their own service pricing, and the PrivateLink service being reached bills its owner separately (endpoint-service hours, cross-region active-region hours)
      notes: The hourly meter survives even a rejected or failed service association -- AWS bills while the endpoint remains provisioned, and a rejected endpoint must be DELETED to stop the meter, not just left unassociated.
