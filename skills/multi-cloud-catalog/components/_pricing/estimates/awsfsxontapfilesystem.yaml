# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awsfsxontapfilesystem
spec:
  presets:
    - preset: 01-single-az-development
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon FSx
          sku_meter: provisioned throughput capacity
          pricing_unit: MiBps-months
          pricing_quantity: "384"
          quantity_basis: the per-HA-pair throughput arm -- total provisioned throughput is the per-pair value times ha_pairs (1 when unstated), every MB/s billed monthly at the deployment type's rate (multi-AZ rates include cross-AZ replication), at the us-east-1 rate since the presets pin region as a placeholder
          list_unit_price: "1.6"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonFSx/20260805125901/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "614.4000"
        - service_name: Amazon FSx
          sku_meter: provisioned SSD storage
          pricing_unit: GB-months
          pricing_quantity: "1024"
          quantity_basis: every provisioned GiB of SSD primary storage bills monthly, full or empty, at the deployment type's rate (multi-AZ carries the standby copy inside a doubled rate); every catalog preset leaves region a placeholder, so this prices the us-east-1 rate
          list_unit_price: "0.125"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonFSx/20260805125901/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "128.0000"
      total_list_cost: "742.4000"
      exclusions:
        - capacity pool storage bills per GB-month actually consumed as volumes tier cold data (a tiering_policy decision on AwsFsxOntapVolume), plus per-operation request charges whenever tiered data is accessed
        - data transfer when clients access the file system from an Availability Zone other than the preferred one is traffic-dependent
        - S3 Access Point requests and bandwidth against the file system bill on S3's own meters, outside this component
      notes: AUTOMATIC SSD IOPS (3 per GiB of storage) are included in the storage rate at no separate charge. ONTAP's compression and deduplication typically shrink what a given SSD provision has to hold (2-5x per the spec's own guidance) -- the lever for this bill is provisioning less capacity, not a discount on the rate.
    - preset: 02-single-az-production
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon FSx
          sku_meter: provisioned throughput capacity
          pricing_unit: MiBps-months
          pricing_quantity: "768"
          quantity_basis: the per-HA-pair throughput arm -- total provisioned throughput is the per-pair value times ha_pairs (1 when unstated), every MB/s billed monthly at the deployment type's rate (multi-AZ rates include cross-AZ replication), at the us-east-1 rate since the presets pin region as a placeholder
          list_unit_price: "1.6"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonFSx/20260805125901/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "1228.8000"
        - service_name: Amazon FSx
          sku_meter: provisioned SSD storage
          pricing_unit: GB-months
          pricing_quantity: "2048"
          quantity_basis: every provisioned GiB of SSD primary storage bills monthly, full or empty, at the deployment type's rate (multi-AZ carries the standby copy inside a doubled rate); every catalog preset leaves region a placeholder, so this prices the us-east-1 rate
          list_unit_price: "0.125"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonFSx/20260805125901/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "256.0000"
      total_list_cost: "1484.8000"
      exclusions:
        - backup storage bills per GB-month of incremental backup data actually retained across the file system's volumes -- churn across the retention window drives it
        - capacity pool storage bills per GB-month actually consumed as volumes tier cold data (a tiering_policy decision on AwsFsxOntapVolume), plus per-operation request charges whenever tiered data is accessed
        - data transfer when clients access the file system from an Availability Zone other than the preferred one is traffic-dependent
        - S3 Access Point requests and bandwidth against the file system bill on S3's own meters, outside this component
      notes: AUTOMATIC SSD IOPS (3 per GiB of storage) are included in the storage rate at no separate charge. ONTAP's compression and deduplication typically shrink what a given SSD provision has to hold (2-5x per the spec's own guidance) -- the lever for this bill is provisioning less capacity, not a discount on the rate.
    - preset: 03-multi-az-high-availability
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon FSx
          sku_meter: provisioned throughput capacity
          pricing_unit: MiBps-months
          pricing_quantity: "768"
          quantity_basis: the per-HA-pair throughput arm -- total provisioned throughput is the per-pair value times ha_pairs (1 when unstated), every MB/s billed monthly at the deployment type's rate (multi-AZ rates include cross-AZ replication), at the us-east-1 rate since the presets pin region as a placeholder
          list_unit_price: "2.5"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonFSx/20260805125901/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "1920.0000"
        - service_name: Amazon FSx
          sku_meter: provisioned SSD storage
          pricing_unit: GB-months
          pricing_quantity: "2048"
          quantity_basis: every provisioned GiB of SSD primary storage bills monthly, full or empty, at the deployment type's rate (multi-AZ carries the standby copy inside a doubled rate); every catalog preset leaves region a placeholder, so this prices the us-east-1 rate
          list_unit_price: "0.25"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonFSx/20260805125901/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "512.0000"
      total_list_cost: "2432.0000"
      exclusions:
        - backup storage bills per GB-month of incremental backup data actually retained across the file system's volumes -- churn across the retention window drives it
        - capacity pool storage bills per GB-month actually consumed as volumes tier cold data (a tiering_policy decision on AwsFsxOntapVolume), plus per-operation request charges whenever tiered data is accessed
        - data transfer when clients access the file system from an Availability Zone other than the preferred one is traffic-dependent
        - S3 Access Point requests and bandwidth against the file system bill on S3's own meters, outside this component
      notes: Multi-AZ replication traffic between the active and standby file servers is included in the throughput rate -- it never appears as a data-transfer charge. AUTOMATIC SSD IOPS (3 per GiB of storage) are included in the storage rate at no separate charge. ONTAP's compression and deduplication typically shrink what a given SSD provision has to hold (2-5x per the spec's own guidance) -- the lever for this bill is provisioning less capacity, not a discount on the rate.
    - preset: 04-single-az-scale-out
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon FSx
          sku_meter: provisioned throughput capacity
          pricing_unit: MiBps-months
          pricing_quantity: "6144"
          quantity_basis: the per-HA-pair throughput arm -- total provisioned throughput is the per-pair value times ha_pairs (1 when unstated), every MB/s billed monthly at the deployment type's rate (multi-AZ rates include cross-AZ replication), at the us-east-1 rate since the presets pin region as a placeholder
          list_unit_price: "1.6"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonFSx/20260805125901/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "9830.4000"
        - service_name: Amazon FSx
          sku_meter: provisioned SSD storage
          pricing_unit: GB-months
          pricing_quantity: "16384"
          quantity_basis: every provisioned GiB of SSD primary storage bills monthly, full or empty, at the deployment type's rate (multi-AZ carries the standby copy inside a doubled rate); every catalog preset leaves region a placeholder, so this prices the us-east-1 rate
          list_unit_price: "0.125"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonFSx/20260805125901/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "2048.0000"
      total_list_cost: "11878.4000"
      exclusions:
        - user-provisioned SSD IOPS bill per IOPS-month only above the included 3 IOPS per GiB of SSD storage -- the billable excess is the provisioned figure minus the capacity-scaled allowance and is not stated here
        - backup storage bills per GB-month of incremental backup data actually retained across the file system's volumes -- churn across the retention window drives it
        - capacity pool storage bills per GB-month actually consumed as volumes tier cold data (a tiering_policy decision on AwsFsxOntapVolume), plus per-operation request charges whenever tiered data is accessed
        - data transfer when clients access the file system from an Availability Zone other than the preferred one is traffic-dependent
        - S3 Access Point requests and bandwidth against the file system bill on S3's own meters, outside this component
      notes: ONTAP's compression and deduplication typically shrink what a given SSD provision has to hold (2-5x per the spec's own guidance) -- the lever for this bill is provisioning less capacity, not a discount on the rate.
