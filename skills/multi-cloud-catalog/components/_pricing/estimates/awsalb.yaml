# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awsalb
spec:
  presets:
    - preset: 01-internet-facing
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Elastic Load Balancing
          sku_meter: Application Load Balancer hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one load balancer, billed every hour of the month
          list_unit_price: "0.0225"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AWSELB/20260720184950/us-east-1/index.json
          retrieved_on: "2026-08-14"
          list_cost: "16.4250"
        - service_name: Amazon Virtual Private Cloud
          sku_meter: public IPv4 address usage
          pricing_unit: IP-hours
          pricing_quantity: "1460"
          quantity_basis: internet-facing -- one public IPv4 node per subnet's Availability Zone, held every hour of the month
          list_unit_price: "0.005"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonVPC/20260724154225/us-east-1/index.json
          retrieved_on: "2026-08-14"
          list_cost: "7.3000"
      total_list_cost: "23.7250"
      exclusions:
        - LCU usage (new connections, active connections, processed bytes, rule evaluations) is traffic-dependent
        - data transfer out to the internet
    - preset: 02-internal-hardened
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Elastic Load Balancing
          sku_meter: Application Load Balancer hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one load balancer, billed every hour of the month
          list_unit_price: "0.0225"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AWSELB/20260720184950/us-east-1/index.json
          retrieved_on: "2026-08-14"
          list_cost: "16.4250"
      total_list_cost: "16.4250"
      exclusions:
        - LCU usage (new connections, active connections, processed bytes, rule evaluations) is traffic-dependent
        - access-log bytes bill to the destination S3 bucket, not the load balancer
      notes: Internal scheme holds no public IPv4 addresses, removing that charge entirely.
    - preset: 03-reserved-capacity
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Elastic Load Balancing
          sku_meter: reserved Load Balancer Capacity Units
          pricing_unit: LCU-hours
          pricing_quantity: "365000"
          quantity_basis: minimum_load_balancer_capacity_units reserves that many LCUs every hour of the month
          list_unit_price: "0.008"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AWSELB/20260720184950/us-east-1/index.json
          retrieved_on: "2026-08-14"
          list_cost: "2920.0000"
        - service_name: Elastic Load Balancing
          sku_meter: Application Load Balancer hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one load balancer, billed every hour of the month
          list_unit_price: "0.0225"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AWSELB/20260720184950/us-east-1/index.json
          retrieved_on: "2026-08-14"
          list_cost: "16.4250"
        - service_name: Amazon Virtual Private Cloud
          sku_meter: public IPv4 address usage
          pricing_unit: IP-hours
          pricing_quantity: "1460"
          quantity_basis: internet-facing -- one public IPv4 node per subnet's Availability Zone, held every hour of the month
          list_unit_price: "0.005"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonVPC/20260724154225/us-east-1/index.json
          retrieved_on: "2026-08-14"
          list_cost: "7.3000"
      total_list_cost: "2943.7250"
      exclusions:
        - LCU usage beyond the reserved LCUs is traffic-dependent
        - data transfer out to the internet
      notes: The reservation is priced as a steady month-long hold; it exists for surge windows and should be removed after them, so real spend depends on how long the reservation stays set.
