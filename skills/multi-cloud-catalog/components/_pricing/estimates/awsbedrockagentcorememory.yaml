# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file joins the component's estimate model (../models/, the authored
# quantities with their derivations) with the provider's price book
# (../pricebook/, pinned unit prices with source URL and retrieval date):
# every line cost and preset total is computed exactly, lines are ordered
# largest cost first, and the catalog-data conformance gate re-verifies all
# arithmetic. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awsbedrockagentcorememory
spec:
  presets:
    - preset: 01-assistant-memory
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - short-term memory bills $0.25 per 1,000 new events as agents write session activity -- volume the manifest cannot see
        - both strategies (SEMANTIC facts, SUMMARIZATION summaries) are built-in, so every extracted record bills $0.75 per 1,000 records stored per month (billed hourly assuming a 31-day month) -- and the record count only grows until something deletes records
        - long-term retrieval bills $0.50 per 1,000 memory record retrievals as agents read
        - no permanent free allotment exists for memory meters -- the up-to-$200 AWS Free Tier credit for new customers is a one-time trial, not a tier
      notes: Zero committed cost -- the memory object, its two strategies, and the 30-day event window bill nothing at rest; every meter starts with the first event an agent writes. The storage meter is the one that accumulates month over month.
    - preset: 02-episodic-memory-with-streaming
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - short-term memory bills $0.25 per 1,000 new events; the EPISODIC built-in strategy stores episodes and reflections at $0.75 per 1,000 records per month (billed hourly assuming a 31-day month); retrievals bill $0.50 per 1,000
        - the Kinesis analytics stream is a REFERENCED component -- its shard or on-demand capacity meters bill on that AwsKinesisStream whether or not records flow; this memory adds no delivery meter of its own (none is published), and METADATA_ONLY keeps delivered payloads small
        - the 90-day event window carries no published meter -- event retention length does not bill
        - no permanent free allotment exists for memory meters -- the up-to-$200 AWS Free Tier credit for new customers is a one-time trial, not a tier
      notes: Zero committed cost ON THIS COMPONENT -- the memory, strategy, indexed key, and delivery wiring bill nothing at rest. The referenced Kinesis stream carries its own always-on capacity meter, but that spend lives (and is estimated) on the AwsKinesisStream component, not here.
