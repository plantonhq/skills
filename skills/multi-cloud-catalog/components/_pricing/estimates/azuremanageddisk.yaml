# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file joins the component's estimate model (../models/, the authored
# quantities with their derivations) with the provider's price book
# (../pricebook/, pinned unit prices with source URL and retrieval date):
# every line cost and preset total is computed exactly, lines are ordered
# largest cost first, and the catalog-data conformance gate re-verifies all
# arithmetic. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azuremanageddisk
spec:
  presets:
    - preset: 01-premium-data-disk
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Storage
          sku_meter: disk tier fee
          pricing_unit: disk-months
          pricing_quantity: "1"
          quantity_basis: PREMIUM_LRS at 256 GiB lands exactly on the P15 tier band, whose flat monthly fee covers the capacity and the tier's fixed 1,100 IOPS / 125 MBps -- one disk-month
          list_unit_price: "38.012142"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Storage' and armRegionName eq 'eastus' and meterName eq 'P15 LRS Disk' and productName eq 'Premium SSD Managed Disks' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "38.012142"
      total_list_cost: "38.012142"
      exclusions:
        - the fee accrues from creation until deletion, attached or not -- an orphaned disk bills at full rate forever
        - snapshots taken of this disk bill their own snapshot storage per GB-month
        - outbound data transfer follows attachment traffic and bills on network meters
      notes: Premium SSD fixed tiers bill by size band -- anything from 129 to 256 GiB pays this same P15 fee, so the preset's 256 GiB uses its band fully.
    - preset: 02-premium-v2-dialed-performance
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Storage
          sku_meter: provisioned IOPS
          pricing_unit: IOPS-hours
          pricing_quantity: "3650000"
          quantity_basis: 8,000 dialed IOPS minus the free 3,000 baseline every v2 disk includes leaves 5,000 billable IOPS -- 5,000 x 730 hours (the retail meter's paid tier starts at 3,000 units, verified 2026-08-18)
          list_unit_price: "0.000007"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Storage' and armRegionName eq 'eastus' and meterName eq 'Premium LRS Provisioned IOPS' and productName eq 'Azure Premium SSD v2' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "25.5500"
        - service_name: Storage
          sku_meter: provisioned capacity
          pricing_unit: GiB-hours
          pricing_quantity: "93440"
          quantity_basis: 128 GiB billed per GiB-hour around the clock -- 128 x 730 hours
          list_unit_price: "0.00011"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Storage' and armRegionName eq 'eastus' and meterName eq 'Premium LRS Provisioned Capacity' and productName eq 'Azure Premium SSD v2' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "10.2784"
        - service_name: Storage
          sku_meter: provisioned throughput
          pricing_unit: MBps-hours
          pricing_quantity: "127750"
          quantity_basis: 300 dialed MBps minus the free 125 MBps baseline leaves 175 billable MBps -- 175 x 730 hours (the retail meter's paid tier starts at 125 units, verified 2026-08-18)
          list_unit_price: "0.000055"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Storage' and armRegionName eq 'eastus' and meterName eq 'Premium LRS Provisioned Throughput (MBps)' and productName eq 'Azure Premium SSD v2' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "7.02625"
      total_list_cost: "42.85465"
      exclusions:
        - the meters accrue from creation until deletion, attached or not
        - snapshots taken of this disk bill their own snapshot storage per GB-month
        - outbound data transfer follows attachment traffic and bills on network meters
      notes: Premium SSD v2 decouples the three dials -- this preset pays for capacity plus exactly the performance dialed above the free baseline, which is the SKU's whole economic argument over fixed tiers.
