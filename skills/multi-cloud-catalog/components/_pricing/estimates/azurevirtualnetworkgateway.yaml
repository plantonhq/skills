# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azurevirtualnetworkgateway
spec:
  presets:
    - preset: 01-site-to-site-vpn
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: VPN Gateway
          sku_meter: gateway compute
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one gateway x 730 hours at the SKU's own hourly rate -- the tier alone fixes the price (the generation knob changes throughput ceilings, not the rate, and an active-active pair bills the same single SKU rate as active-standby)
          list_unit_price: "0.21"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'VPN Gateway' and armRegionName eq 'eastus' and meterName eq 'VpnGw1AZ' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "153.3000"
      total_list_cost: "153.3000"
      exclusions:
        - site-to-site tunnels beyond the included 10 bill per tunnel-hour, counted across ALL the gateway's connections -- an aggregate invisible in this manifest
        - egress data transfer bills at standard rates and follows traffic (same-region VNet-to-VNet traffic through the gateway is free in both directions)
        - the referenced public IPs and the GatewaySubnet's virtual network are first-class components billing on their own lines
    - preset: 02-active-active-zone-redundant
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: VPN Gateway
          sku_meter: gateway compute
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one gateway x 730 hours at the SKU's own hourly rate -- the tier alone fixes the price (the generation knob changes throughput ceilings, not the rate, and an active-active pair bills the same single SKU rate as active-standby)
          list_unit_price: "0.54"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'VPN Gateway' and armRegionName eq 'eastus' and meterName eq 'VpnGw2AZ' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "394.2000"
      total_list_cost: "394.2000"
      exclusions:
        - site-to-site tunnels beyond the included 10 bill per tunnel-hour, counted across ALL the gateway's connections -- an aggregate invisible in this manifest
        - egress data transfer bills at standard rates and follows traffic (same-region VNet-to-VNet traffic through the gateway is free in both directions)
        - the referenced public IPs and the GatewaySubnet's virtual network are first-class components billing on their own lines
      notes: Active-active adds no gateway spend -- Microsoft bills one SKU rate however many instances are live; only the second public IP adds a line, on its own component.
    - preset: 03-point-to-site-entra-id
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: VPN Gateway
          sku_meter: gateway compute
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one gateway x 730 hours at the SKU's own hourly rate -- the tier alone fixes the price (the generation knob changes throughput ceilings, not the rate, and an active-active pair bills the same single SKU rate as active-standby)
          list_unit_price: "0.21"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'VPN Gateway' and armRegionName eq 'eastus' and meterName eq 'VpnGw1AZ' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "153.3000"
      total_list_cost: "153.3000"
      exclusions:
        - site-to-site tunnels beyond the included 10 bill per tunnel-hour, counted across ALL the gateway's connections -- an aggregate invisible in this manifest
        - point-to-site connections beyond the included 128 bill per connection-hour and follow client behavior
        - egress data transfer bills at standard rates and follows traffic (same-region VNet-to-VNet traffic through the gateway is free in both directions)
        - the referenced public IPs and the GatewaySubnet's virtual network are first-class components billing on their own lines
