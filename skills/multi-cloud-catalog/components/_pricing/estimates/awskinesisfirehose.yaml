# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file joins the component's estimate model (../models/, the authored
# quantities with their derivations) with the provider's price book
# (../pricebook/, pinned unit prices with source URL and retrieval date):
# every line cost and preset total is computed exactly, lines are ordered
# largest cost first, and the catalog-data conformance gate re-verifies all
# arithmetic. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awskinesisfirehose
spec:
  presets:
    - preset: 01-s3-data-lake
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - Direct PUT ingestion bills per GB at tiered rates with every record rounded UP to the next 5 KB -- small-event workloads pay a multiple of their true bytes, and Firehose has no free allotment
        - delivered objects bill S3 PUT requests and storage on the destination bucket's component; GZIP compression shrinks what lands there at no Firehose charge
      notes: Zero committed cost -- the stream is delivery plumbing, and the ingestion meter starts with the first PutRecord; the 5 KB rounding rule makes producer-side batching the biggest lever.
    - preset: 02-opensearch-log-analytics
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - Direct PUT ingestion bills per GB with 5 KB per-record rounding
        - no vpc_config is set, so the per-AZ-hour VPC delivery meter -- the one charge that would run traffic-or-none -- never starts; delivery goes to the domain's public endpoint
        - the OpenSearch domain's instances and storage bill on the referenced AwsOpenSearchDomain component
        - failed-document backups bill S3 requests and storage on the backup bucket
      notes: Zero committed cost -- log volume drives everything; the heavy money in this pipeline is the OpenSearch domain itself, billing on its own component.
    - preset: 03-http-endpoint-webhook
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - Direct PUT ingestion bills per GB with 5 KB per-record rounding
        - data transfer out of AWS to the Datadog intake endpoint bills per GB on top of ingestion
        - failed-data backups bill S3 requests and storage on the backup bucket; Datadog's own ingestion pricing is external to AWS entirely
      notes: Zero committed cost -- an idle stream to a third-party endpoint bills nothing; every meter (ingestion, egress, backup) follows the log volume applications actually send.
    - preset: 04-s3-parquet-analytics
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - Kinesis-stream-source ingestion bills per GB with 5 KB per-record rounding; the source stream's own shard or on-demand meters bill on its AwsKinesisStream component
        - JSON-to-Parquet format conversion adds its per-GB charge on the same rounded volume
        - dynamic partitioning stacks per GB processed PLUS per 1,000 S3 objects delivered -- the 64 MiB buffer ceiling holds the object count down -- plus per-hour JQ processing while the metadata-extraction parser runs
        - delivered Parquet bills S3 requests and storage on the data-lake bucket; the Glue schema registry bills on its own service
      notes: Zero committed cost -- this is the meter-stacking preset (ingestion, conversion, partitioning, JQ all per-GB arms), but every arm follows the stream's traffic and commits nothing at creation.
    - preset: 05-snowflake-streaming
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - Snowflake delivery bills per GB processed with NO 5 KB rounding, metered on the higher of ingested and delivered bytes
        - Snowpipe Streaming's own ingest costs bill on the Snowflake side, outside AWS
        - the key-pair credential secret bills on Secrets Manager; failed-data backups bill on the S3 backup bucket
      notes: Zero committed cost -- the unrounded per-GB rate makes this shape fairer to small records than Direct PUT-to-S3, and nothing bills until producers send data.
    - preset: 06-iceberg-lakehouse
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - Iceberg delivery bills per GB with NO 5 KB rounding; upsert semantics (unique_keys on order_id) change write behavior, not the rate
        - the Iceberg tables' underlying S3 storage and the Glue Data Catalog bill on their own components
        - failed-data backups bill S3 requests and storage on the backup bucket
      notes: Zero committed cost -- change-data-capture into the lakehouse meters only the bytes that flow; the table storage the upserts accumulate lives on the destination's own bill.
