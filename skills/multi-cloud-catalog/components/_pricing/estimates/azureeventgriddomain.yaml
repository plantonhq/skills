# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file joins the component's estimate model (../models/, the authored
# quantities with their derivations) with the provider's price book
# (../pricebook/, pinned unit prices with source URL and retrieval date):
# every line cost and preset total is computed exactly, lines are ordered
# largest cost first, and the catalog-data conformance gate re-verifies all
# arithmetic. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azureeventgriddomain
spec:
  presets:
    - preset: 01-multi-tenant-domain
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - every event published to the domain endpoint -- whichever tenant's topic it names -- bills 0.60 USD per million operations past the monthly 100,000-operation free grant, counted in 64 KB units
        - every delivery attempt by subscriptions on any domain topic bills the same meter, retries included; advanced filtering and subject-ends-with filtering bill per evaluation (event-type matching and subject-begins-with are free)
        - auto-managed domain topics carry no meter -- Azure materializing one per tenant on first subscription and removing it on last unsubscribe moves nothing on the invoice
        - dead-lettered events bill as delivery operations, with blob storage charges on the destination storage account's own component
      notes: "Zero committed cost -- the domain has no meter for existing, and tenant count is free: only the aggregate operation volume across all tenants bills, at the same rate one custom topic would pay."
    - preset: 02-pinned-topics-domain
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - every event published to the domain endpoint bills 0.60 USD per million operations past the monthly 100,000-operation free grant, counted in 64 KB units -- Entra-only auth is free posture that moves no meter
        - every delivery attempt by subscriptions on any pinned topic bills the same meter, retries included; advanced filtering and subject-ends-with filtering bill per evaluation
        - pinned domain topics (declared AzureEventgridDomainTopic resources) carry no meter at any count -- governance costs nothing
        - dead-lettered events bill as delivery operations, with blob storage charges on the destination storage account's own component
      notes: Zero committed cost -- the pinned-topics posture and disabled access keys change who may publish and which topics exist, never what an operation costs.
