# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azurevirtualmachine
spec:
  presets:
    - preset: 01-ubuntu-ssh
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual Machines
          sku_meter: VM compute
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one VM x 730 hours at the size's base pay-as-you-go rate (the Linux rate; Windows fleets reach this line only with Azure Hybrid Benefit, whose license travels with the customer, not the meter)
          list_unit_price: "0.096"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual Machines' and armRegionName eq 'eastus' and armSkuName eq 'Standard_D2s_v3' and meterName eq 'D2s v3' and productName eq 'Virtual Machines DSv3 Series' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-16"
          list_cost: "70.0800"
      total_list_cost: "70.0800"
      exclusions:
        - the OS disk bills at the managed-disk tier matching the image's inherited size, which the manifest does not carry
        - referenced data disks, network interfaces, and public IPs are first-class components billing on their own lines
        - network egress follows traffic and client placement
    - preset: 02-windows-server
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual Machines
          sku_meter: VM compute
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one VM x 730 hours at the size's base pay-as-you-go rate (the Linux rate; Windows fleets reach this line only with Azure Hybrid Benefit, whose license travels with the customer, not the meter)
          list_unit_price: "0.096"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual Machines' and armRegionName eq 'eastus' and armSkuName eq 'Standard_D2s_v3' and meterName eq 'D2s v3' and productName eq 'Virtual Machines DSv3 Series' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-16"
          list_cost: "70.0800"
        - service_name: Storage
          sku_meter: OS disk storage
          pricing_unit: disk-months
          pricing_quantity: "1"
          quantity_basis: a 128 GiB Premium SSD OS disk is exactly the P10 tier, billed per disk-month
          list_unit_price: "19.71"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Storage' and armRegionName eq 'eastus' and meterName eq 'P10 LRS Disk' and productName eq 'Premium SSD Managed Disks' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "19.7100"
      total_list_cost: "89.7900"
      exclusions:
        - referenced data disks, network interfaces, and public IPs are first-class components billing on their own lines
        - network egress follows traffic and client placement
      notes: Azure Hybrid Benefit prices the machine at the base compute rate; the Windows Server license obligation is carried by the customer's own licensing, not this bill.
