# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awsbudget
spec:
  presets:
    - preset: 01-monthly-cost-guardrail
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - AWS Budgets Reports bill $0.01 per delivered report -- a console feature outside this spec
        - SNS topics subscribed to notifications bill on their own components; email subscribers are free
      notes: The AWSBudgets offer prices the account's first 62 action-enabled budget-days each month at $0.00 -- two action-enabled budgets running a full month are free. Whether THIS budget falls inside that account-level allowance no single manifest can see, so the estimate prices the full $0.10 rate and states the allowance (the adjudicated treatment, same as the CloudWatch dashboard and alarm allowances). A notifications-only budget (the 01 preset) is genuinely free -- the offer's plain Budget-days meter prices $0.00 flat at any budget count. Budgets is account-global; the offer publishes one global rate (location "Any"), so the region assumption only anchors the book lookup.
    - preset: 02-enforced-sandbox-budget
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: AWSBudgets
          sku_meter: action-enabled budget days
          pricing_unit: Budget-days
          pricing_quantity: "30.42"
          quantity_basis: a budget with any action attached bills per budget-day it exists, however many actions it carries -- 730 hours / 24 = 30.42 budget-days a month, priced at the full marginal rate past the account's free allowance
          list_unit_price: "0.1"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AWSBudgets/20221110130741/index.json
          retrieved_on: "2026-08-19"
          list_cost: "3.0420"
      total_list_cost: "3.0420"
      exclusions:
        - AWS Budgets Reports bill $0.01 per delivered report -- a console feature outside this spec
        - SNS topics subscribed to notifications bill on their own components; email subscribers are free
        - what a fired action DOES (denying IAM groups, attaching SCPs, stopping instances via SSM) changes other components' bills, not this one's
      notes: The AWSBudgets offer prices the account's first 62 action-enabled budget-days each month at $0.00 -- two action-enabled budgets running a full month are free. Whether THIS budget falls inside that account-level allowance no single manifest can see, so the estimate prices the full $0.10 rate and states the allowance (the adjudicated treatment, same as the CloudWatch dashboard and alarm allowances). A notifications-only budget (the 01 preset) is genuinely free -- the offer's plain Budget-days meter prices $0.00 flat at any budget count. Budgets is account-global; the offer publishes one global rate (location "Any"), so the region assumption only anchors the book lookup.
