# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its capacity derivation
# (../capacity/, the machine-executable value-to-capacity rules): each
# preset states the CPU/memory requests and limits and the persistent
# volume storage it reserves from its target cluster, with a basis sentence
# tracing every total back to the manifest's values. Cluster-capacity
# components carry no priced lines -- the dollar value of the reservation
# is the target cluster's node and storage-class economics, which this
# catalog cannot know. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: kubernetesargoworkflows
spec:
  presets:
    - preset: 02-durable-pipelines
      capacity_footprint:
        cpu_requests: 500m
        memory_requests: 512Mi
        basis: 1 workflow controller x (500m CPU / 512Mi memory requests)
      exclusions:
        - the workflow pods are the real capacity story -- every Workflow step runs as its own pod, sized by the workflow's declarations plus the argoexec executor, created at run time and gone at completion; no manifest-time statement can see them
        - the dollar value of this capacity is the target cluster's node and storage-class economics
        - components with no declared resources carry the chart defaults, and this chart ships NO default requests or limits for the controller or server -- unsized pods schedule unbounded and reserve nothing, so a zero line is the literal truth, not a free component
        - helm_values merges LAST over the typed rendering and can override modeled values, including resources, replicas, and the executor's sizing -- this footprint describes the spec's declaration
        - artifact and archived-log bytes grow with pipeline activity on the declared object store -- on a composed KubernetesSeaweedFs that capacity rides the SeaweedFS component's own footprint; on a cloud bucket it is that provider's storage bill
        - the workflow archive writes history to the declared relational database -- its capacity rides the composed KubernetesPostgres/KubernetesMysql resource (or the external database), never this footprint
      notes: controller replicas beyond 1 are hot standbys behind leader election -- they take over on failure, they do not share the reconcile workload, and each reserves the full controller resources
