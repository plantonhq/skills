# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azurefirewall
spec:
  presets:
    - preset: 01-hub-spoke-egress
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Azure Firewall
          sku_meter: firewall deployment hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: a STANDARD firewall bills its flat deployment fee every hour it exists (x 730), regardless of scale -- Azure autoscales the underlying instances without changing this meter, and availability zones add nothing
          list_unit_price: "1.25"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Azure Firewall' and armRegionName eq 'eastus' and meterName eq 'Standard Deployment' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "912.5000"
      total_list_cost: "912.5000"
      exclusions:
        - data processing bills every GB the firewall inspects at the tier's per-GB rate -- the variable half of every firewall bill, following traffic
        - the referenced public IPs bill hourly on their own components
        - the attached AzureFirewallPolicy carries its own billing rules on its own component
        - prescaled capacity units are an opt-in reservation configured outside this spec; by default the firewall bills no capacity-unit charges
      notes: Zone redundancy is free on Azure Firewall -- spanning availability zones changes availability posture, not the deployment meter.
    - preset: 02-forced-tunneling
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Azure Firewall
          sku_meter: firewall deployment hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: a STANDARD firewall bills its flat deployment fee every hour it exists (x 730), regardless of scale -- Azure autoscales the underlying instances without changing this meter, and availability zones add nothing
          list_unit_price: "1.25"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Azure Firewall' and armRegionName eq 'eastus' and meterName eq 'Standard Deployment' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "912.5000"
      total_list_cost: "912.5000"
      exclusions:
        - data processing bills every GB the firewall inspects at the tier's per-GB rate -- the variable half of every firewall bill, following traffic
        - the referenced public IPs bill hourly on their own components
        - the attached AzureFirewallPolicy carries its own billing rules on its own component
        - prescaled capacity units are an opt-in reservation configured outside this spec; by default the firewall bills no capacity-unit charges
      notes: Zone redundancy is free on Azure Firewall -- spanning availability zones changes availability posture, not the deployment meter.
