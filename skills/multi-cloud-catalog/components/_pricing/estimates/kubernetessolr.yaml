# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its capacity derivation
# (../capacity/, the machine-executable value-to-capacity rules): each
# preset states the CPU/memory requests and limits and the persistent
# volume storage it reserves from its target cluster, with a basis sentence
# tracing every total back to the manifest's values. Cluster-capacity
# components carry no priced lines -- the dollar value of the reservation
# is the target cluster's node and storage-class economics, which this
# catalog cannot know. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: kubernetessolr
spec:
  presets:
    - preset: 01-dev-single-node
      capacity_footprint:
        cpu_requests: 250m
        memory_requests: 768Mi
        cpu_limits: "1"
        memory_limits: 1Gi
        basis: 1 node x (250m CPU / 768Mi memory requests, 1 CPU / 1Gi limits)
      exclusions:
        - the dollar value of this capacity is the target cluster's node and storage-class economics
        - the bundled ZooKeeper ensemble members run with the zookeeper-operator's default container resources when the manifest declares none -- a real reservation this footprint cannot count
      notes: without declared persistent storage the Solr nodes run on ephemeral emptyDir (the operator default) -- no volume is reserved, and indices are lost when a pod leaves its node each bundled ZooKeeper member provisions a 20Gi volume by default (the zookeeper-operator default) when no size is declared -- a reservation this footprint cannot count
    - preset: 02-production-cloud
      capacity_footprint:
        cpu_requests: "6"
        memory_requests: 12Gi
        cpu_limits: "12"
        memory_limits: 12Gi
        persistent_storage: 330Gi
        basis: "3 nodes x (2 CPU / 4Gi memory requests, 4 CPU / 4Gi limits) + 3 x 100Gi data volumes; 3 zookeeper members: 3 x 10Gi data volumes"
      exclusions:
        - the dollar value of this capacity is the target cluster's node and storage-class economics
        - the bundled ZooKeeper ensemble members run with the zookeeper-operator's default container resources when the manifest declares none -- a real reservation this footprint cannot count
    - preset: 03-s3-backups
      capacity_footprint:
        cpu_requests: "6"
        memory_requests: 12Gi
        cpu_limits: "12"
        memory_limits: 12Gi
        persistent_storage: 300Gi
        basis: 3 nodes x (2 CPU / 4Gi memory requests, 4 CPU / 4Gi limits) + 3 x 100Gi data volumes
      exclusions:
        - the dollar value of this capacity is the target cluster's node and storage-class economics
        - the bundled ZooKeeper ensemble members run with the zookeeper-operator's default container resources when the manifest declares none -- a real reservation this footprint cannot count
        - backup data written to registered repositories lands in external object storage or a shared volume, outside this cluster's capacity
      notes: each bundled ZooKeeper member provisions a 20Gi volume by default (the zookeeper-operator default) when no size is declared -- a reservation this footprint cannot count
