# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azurevpngatewayconnection
spec:
  presets:
    - preset: 01-standard-branch-connection
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual WAN
          sku_meter: VPN S2S connection unit
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one connected branch is one connection unit x 730 hours -- the unit counts branch sites, not tunnels, so a two-link active-active connection bills the same as a single-link one; the region rides the owning gateway and is assumed eastus
          list_unit_price: "0.05"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual WAN' and armRegionName eq 'eastus' and meterName eq 'VPN S2S Connection Unit' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "36.5000"
      total_list_cost: "36.5000"
      exclusions:
        - data leaving Azure through the tunnels bills at standard data-transfer rates and follows traffic
        - the gateway's scale units (capacity) bill on the AzureVpnGateway component; the hub's deployment hours and data processing bill on the AzureVirtualHub
    - preset: 02-pinned-ipsec-connection
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual WAN
          sku_meter: VPN S2S connection unit
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one connected branch is one connection unit x 730 hours -- the unit counts branch sites, not tunnels, so a two-link active-active connection bills the same as a single-link one; the region rides the owning gateway and is assumed eastus
          list_unit_price: "0.05"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual WAN' and armRegionName eq 'eastus' and meterName eq 'VPN S2S Connection Unit' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "36.5000"
      total_list_cost: "36.5000"
      exclusions:
        - data leaving Azure through the tunnels bills at standard data-transfer rates and follows traffic
        - the gateway's scale units (capacity) bill on the AzureVpnGateway component; the hub's deployment hours and data processing bill on the AzureVirtualHub
