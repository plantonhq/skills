# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awsmanagedprometheusscraper
spec:
  presets:
    - preset: 01-eks-default-scraper
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: AmazonPrometheus
          sku_meter: collector hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one scraper is one agentless collector, billed per hour around the clock from provisioning to deletion -- there is no stop switch
          list_unit_price: "0.04"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonPrometheus/20260804172725/us-east-1/index.json
          retrieved_on: "2026-08-19"
          list_cost: "29.2000"
      total_list_cost: "29.2000"
      exclusions:
        - metric samples collected bill $0.03 per 10M (us-east-1) on top of collector hours -- volume follows scrape_interval and target count in the scrape configuration and the size of the scraped fleet
        - ingestion, storage, and query charges for the delivered metrics bill on the destination AMP workspace, not on this component (the workspace's first 40M ingested samples per month are free, then tiered from $0.90 per 10M)
      notes: Creates run long (up to 30 minutes of collector provisioning) and deletes drain (up to 20) -- the meter follows the collector's existence, so the first and last partial hours are billed like any others.
    - preset: 02-vpc-static-targets
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: AmazonPrometheus
          sku_meter: collector hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one scraper is one agentless collector, billed per hour around the clock from provisioning to deletion -- there is no stop switch
          list_unit_price: "0.04"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonPrometheus/20260804172725/us-east-1/index.json
          retrieved_on: "2026-08-19"
          list_cost: "29.2000"
      total_list_cost: "29.2000"
      exclusions:
        - metric samples collected bill $0.03 per 10M (us-east-1) on top of collector hours -- volume follows scrape_interval and target count in the scrape configuration and the size of the scraped fleet
        - ingestion, storage, and query charges for the delivered metrics bill on the destination AMP workspace, not on this component (the workspace's first 40M ingested samples per month are free, then tiered from $0.90 per 10M)
      notes: Creates run long (up to 30 minutes of collector provisioning) and deletes drain (up to 20) -- the meter follows the collector's existence, so the first and last partial hours are billed like any others.
