# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file joins the component's estimate model (../models/, the authored
# quantities with their derivations) with the provider's price book
# (../pricebook/, pinned unit prices with source URL and retrieval date):
# every line cost and preset total is computed exactly, lines are ordered
# largest cost first, and the catalog-data conformance gate re-verifies all
# arithmetic. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: kubernetesclickhouse
spec:
  presets:
    - preset: 01-dev-minimal
      capacity_footprint:
        cpu_requests: 500m
        memory_requests: 1Gi
        cpu_limits: "2"
        memory_limits: 4Gi
        persistent_storage: 20Gi
        basis: 1 host (1 shard x 1 replica, the spec defaults) x (500m CPU / 1Gi memory requests, 2 CPU / 4Gi limits) + 1 x 20Gi data volume; no Keeper -- a single-host topology triggers no coordination
      exclusions:
        - the dollar value of this capacity is the target cluster's node and storage-class economics
    - preset: 02-production-replicated
      capacity_footprint:
        cpu_requests: 6300m
        memory_requests: 25344Mi
        cpu_limits: 13500m
        memory_limits: 51Gi
        persistent_storage: 630Gi
        basis: 3 hosts (1 shard x 3 replicas) x (2 CPU / 8Gi memory requests, 4 CPU / 16Gi limits) + 3 x 200Gi data volumes, plus the managed Keeper's 3 nodes x (100m CPU / 256Mi memory requests, 500m CPU / 1Gi limits) + 3 x 10Gi volumes
      exclusions:
        - the dollar value of this capacity is the target cluster's node and storage-class economics
    - preset: 03-sharded-analytics
      capacity_footprint:
        cpu_requests: "32"
        memory_requests: 128Gi
        cpu_limits: "64"
        memory_limits: 256Gi
        persistent_storage: 4030Gi
        basis: 8 hosts (4 shards x 2 replicas) x (4 CPU / 16Gi memory requests, 8 CPU / 32Gi limits) + 8 x 500Gi data volumes, plus the managed Keeper's 3 x 10Gi volumes -- the preset's Keeper declares no CPU or memory resources, so its pods run on operator defaults and add storage only to this footprint
      exclusions:
        - the dollar value of this capacity is the target cluster's node and storage-class economics
        - the Keeper pods carry no declared CPU or memory requests in this preset -- their runtime consumption is real but unreserved
