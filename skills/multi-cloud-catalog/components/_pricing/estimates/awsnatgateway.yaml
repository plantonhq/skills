# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awsnatgateway
spec:
  presets:
    - preset: 01-public-nat-gateway
      region_assumption: us-west-2
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Virtual Private Cloud
          sku_meter: NAT gateway hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one zonal gateway, billed every hour of the month
          list_unit_price: "0.045"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonEC2/20260814011739/us-west-2/index.json
          retrieved_on: "2026-08-14"
          list_cost: "32.8500"
      total_list_cost: "32.8500"
      exclusions:
        - data processing bills per GB in both directions -- the dominant real-world cost, entirely traffic-dependent
        - the attached Elastic IP bills as a public IPv4 address on its own AwsElasticIp component
        - cross-AZ transfer applies for traffic reaching the gateway from other zones
      notes: The hourly charge is the committed floor; a chatty private subnet can multiply it through data processing.
    - preset: 02-private-nat-gateway
      region_assumption: us-west-2
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Virtual Private Cloud
          sku_meter: NAT gateway hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one zonal gateway, billed every hour of the month
          list_unit_price: "0.045"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonEC2/20260814011739/us-west-2/index.json
          retrieved_on: "2026-08-14"
          list_cost: "32.8500"
      total_list_cost: "32.8500"
      exclusions:
        - data processing bills per GB in both directions -- the dominant real-world cost, entirely traffic-dependent
        - cross-AZ transfer applies for traffic reaching the gateway from other zones
      notes: Private connectivity holds no public IPv4 addresses, removing that charge entirely -- the NAT rates are unchanged.
    - preset: 03-regional-nat-gateway
      region_assumption: us-west-2
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Virtual Private Cloud
          sku_meter: NAT gateway hours
          pricing_unit: hours
          pricing_quantity: "1460"
          quantity_basis: a regional gateway bills NAT hours once per Availability Zone it spans; priced at the two-AZ minimum of a multi-AZ VPC, 2 x 730 hours
          list_unit_price: "0.045"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonEC2/20260814011739/us-west-2/index.json
          retrieved_on: "2026-08-14"
          list_cost: "65.7000"
      total_list_cost: "65.7000"
      exclusions:
        - data processing bills per GB in both directions -- the dominant real-world cost, entirely traffic-dependent
        - each additional Availability Zone in the VPC adds another 730 gateway-hours per month
        - the AWS-managed public IPv4 addresses (one per zone) bill per address
      notes: Estimated at the smallest multi-AZ layout; a three-AZ VPC raises the gateway-hours line by half again.
