# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file joins the component's estimate model (../models/, the authored
# quantities with their derivations) with the provider's price book
# (../pricebook/, pinned unit prices with source URL and retrieval date):
# every line cost and preset total is computed exactly, lines are ordered
# largest cost first, and the catalog-data conformance gate re-verifies all
# arithmetic. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: gcpartifactregistryrepo
spec:
  presets:
    - preset: 01-docker-standard
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - an empty repository bills nothing -- stored images accrue $0.000136986 per GiB-hour past the billing account's shared 0.5 GiB-month free tier as CI pushes them, and the delete-old-untagged plus keep-last-10 cleanup policies are the brake on unbounded growth
        - data transfer bills per GiB delivered when pulls cross locations; same-location pulls by co-located GKE/Cloud Run workloads are free
        - if the project's Container Scanning API is on, each newly pushed digest scans once at $0.26
      notes: Creation is free; the estimate is zero because the manifest commits no bytes -- a busy CI pipeline's spend follows its push volume and pull topology.
    - preset: 02-remote-docker-hub-cache
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - the cache stores a copy of every upstream image pulled through it, billing the same $0.000136986 per GiB-hour past the account's 0.5 GiB-month free tier; the 30-day expire-cache policy drops anything not re-pulled
        - upstream fetches from Docker Hub are data transfer INTO Google Cloud, which is free; delivery to pullers bills per GiB when it crosses locations
        - vulnerability scanning, if enabled on the project, bills $0.26 per newly cached digest
      notes: An unused cache bills nothing -- storage follows what workloads actually pull through it, and the expiry policy keeps the steady state proportional to the working set rather than history.
    - preset: 03-virtual-aggregation
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - Google's pricing page states it directly -- virtual repositories do not store artifacts, so usage costs are for the upstream standard and remote repositories rather than the virtual repository itself
        - the upstream team-images and dockerhub-cache repositories (separate resources) carry the storage and scanning meters for everything this endpoint serves
        - data transfer for delivered artifacts follows the same per-GiB location rules as any repository
      notes: The virtual endpoint is the free front door by design -- it commits nothing and stores nothing; its cost story lives entirely in its upstreams.
