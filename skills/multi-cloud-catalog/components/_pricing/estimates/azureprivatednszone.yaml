# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azureprivatednszone
spec:
  presets:
    - preset: 01-privatelink-zone
      region_assumption: global
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Azure DNS
          sku_meter: hosted private zone
          pricing_unit: zone-months
          pricing_quantity: "1"
          quantity_basis: one private zone billed per month (prorated daily by Azure) at the first-25-zones list rate -- a stated assumption, since the tier is counted across the whole subscription and invisible here; zones from the subscription's 26th bill at a fifth of this rate, so the line never understates a single zone's fee
          list_unit_price: "0.5"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Azure DNS' and armRegionName eq 'Zone 1' and meterName eq 'Private Zone' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "0.5000"
      total_list_cost: "0.5000"
      exclusions:
        - query volume bills per million queries answered, aggregated across the subscription, and follows traffic entirely
        - record sets beyond the first 25,000 in the zone bill a small monthly fee each -- the records ride their own record components, and virtual network links carry no meter at all
    - preset: 02-internal-zone
      region_assumption: global
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Azure DNS
          sku_meter: hosted private zone
          pricing_unit: zone-months
          pricing_quantity: "1"
          quantity_basis: one private zone billed per month (prorated daily by Azure) at the first-25-zones list rate -- a stated assumption, since the tier is counted across the whole subscription and invisible here; zones from the subscription's 26th bill at a fifth of this rate, so the line never understates a single zone's fee
          list_unit_price: "0.5"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Azure DNS' and armRegionName eq 'Zone 1' and meterName eq 'Private Zone' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "0.5000"
      total_list_cost: "0.5000"
      exclusions:
        - query volume bills per million queries answered, aggregated across the subscription, and follows traffic entirely
        - record sets beyond the first 25,000 in the zone bill a small monthly fee each -- the records ride their own record components, and virtual network links carry no meter at all
      notes: SOA tuning changes resolver caching behavior, never the fee -- the customized zone bills identically to the bare one.
