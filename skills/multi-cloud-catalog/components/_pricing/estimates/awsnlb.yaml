# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awsnlb
spec:
  presets:
    - preset: 01-internal
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Elastic Load Balancing
          sku_meter: Network Load Balancer hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one load balancer, billed every hour of the month; every catalog preset leaves region a placeholder, so this prices the us-east-1 rate
          list_unit_price: "0.0225"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AWSELB/20260720184950/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "16.4250"
      total_list_cost: "16.4250"
      exclusions:
        - NLCU usage (billed per used Network load balancer capacity unit-hour) is traffic-dependent
      notes: Internal scheme holds no public IPv4 addresses, removing that charge entirely.
    - preset: 02-static-ip-internet-facing
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Elastic Load Balancing
          sku_meter: Network Load Balancer hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one load balancer, billed every hour of the month; every catalog preset leaves region a placeholder, so this prices the us-east-1 rate
          list_unit_price: "0.0225"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AWSELB/20260720184950/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "16.4250"
        - service_name: Amazon Virtual Private Cloud
          sku_meter: public IPv4 address usage
          pricing_unit: IP-hours
          pricing_quantity: "1460"
          quantity_basis: internet-facing -- one public IPv4 node per subnet mapping's Availability Zone, held every hour of the month; the in-use rate applies whether the address is AWS-assigned or a pinned Elastic IP
          list_unit_price: "0.005"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonVPC/20260724154225/us-east-1/index.json
          retrieved_on: "2026-08-14"
          list_cost: "7.3000"
      total_list_cost: "23.7250"
      exclusions:
        - NLCU usage (billed per used Network load balancer capacity unit-hour) is traffic-dependent
        - data transfer out to the internet
      notes: The public IPv4 address hours are counted here on the load balancer, whether AWS assigns the addresses or subnet mappings pin Elastic IPs -- the in-use rate is the same either way.
    - preset: 03-private-link-hardened
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Elastic Load Balancing
          sku_meter: Network Load Balancer hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one load balancer, billed every hour of the month; every catalog preset leaves region a placeholder, so this prices the us-east-1 rate
          list_unit_price: "0.0225"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AWSELB/20260720184950/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "16.4250"
      total_list_cost: "16.4250"
      exclusions:
        - NLCU usage (billed per used Network load balancer capacity unit-hour) is traffic-dependent
        - access-log bytes (TLS listeners only) bill to the destination S3 bucket, not the load balancer
      notes: Internal scheme holds no public IPv4 addresses, removing that charge entirely.
