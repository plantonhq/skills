# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awsmwaaenvironment
spec:
  presets:
    - preset: 01-basic-private-airflow
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Managed Workflows for Apache Airflow
          sku_meter: environment instance hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one environment billed at its class rate every hour of the month -- the fee includes one worker, two schedulers, and two web servers; AWS defaults the class to mw1.small when the manifest does not state it
          list_unit_price: "0.49"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonMWAA/20260420214526/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "357.7000"
      total_list_cost: "357.7000"
      exclusions:
        - autoscaled workers between the min_workers floor and the max_workers ceiling bill per hour only while task queue depth demands them
        - autoscaled web servers between the min_webservers floor and the max_webservers ceiling bill per hour only while UI and REST-API load demands them
        - metadata database storage bills per GB-month as workflow history accumulates
        - the DAG source bucket, execution role, and KMS key bill on their own components
      notes: Estimated at the scaling floors -- the committed always-on spend; the environment fee already includes one worker, two schedulers, and two web servers.
    - preset: 02-production-encrypted-logging
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Managed Workflows for Apache Airflow
          sku_meter: environment instance hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one environment billed at its class rate every hour of the month -- the fee includes one worker, two schedulers, and two web servers; AWS defaults the class to mw1.small when the manifest does not state it
          list_unit_price: "0.74"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonMWAA/20260420214526/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "540.2000"
        - service_name: Amazon Managed Workflows for Apache Airflow
          sku_meter: additional worker instance hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: min_workers holds 2 always-on workers; the environment fee includes one, so 1 additional worker bills every hour of the month at the class rate (class assumed mw1.small when unset)
          list_unit_price: "0.11"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonMWAA/20260420214526/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "80.3000"
        - service_name: Amazon Managed Workflows for Apache Airflow
          sku_meter: additional scheduler instance hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: 3 schedulers run for the environment's lifetime; the environment fee includes two, so 1 additional scheduler bills every hour of the month at the class rate (class assumed mw1.small when unset)
          list_unit_price: "0.11"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonMWAA/20260420214526/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "80.3000"
      total_list_cost: "700.8000"
      exclusions:
        - autoscaled workers between the min_workers floor and the max_workers ceiling bill per hour only while task queue depth demands them
        - autoscaled web servers between the min_webservers floor and the max_webservers ceiling bill per hour only while UI and REST-API load demands them
        - metadata database storage bills per GB-month as workflow history accumulates
        - CloudWatch Logs ingestion and storage for the enabled log modules depend on log level and DAG volume
        - the DAG source bucket, execution role, and KMS key bill on their own components
      notes: Estimated at the scaling floors -- the committed always-on spend; the environment fee already includes one worker, two schedulers, and two web servers.
    - preset: 03-public-access-with-plugins
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Managed Workflows for Apache Airflow
          sku_meter: environment instance hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one environment billed at its class rate every hour of the month -- the fee includes one worker, two schedulers, and two web servers; AWS defaults the class to mw1.small when the manifest does not state it
          list_unit_price: "0.99"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonMWAA/20260420214526/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "722.7000"
        - service_name: Amazon Managed Workflows for Apache Airflow
          sku_meter: additional scheduler instance hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: 3 schedulers run for the environment's lifetime; the environment fee includes two, so 1 additional scheduler bills every hour of the month at the class rate (class assumed mw1.small when unset)
          list_unit_price: "0.22"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonMWAA/20260420214526/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "160.6000"
      total_list_cost: "883.3000"
      exclusions:
        - autoscaled workers between the min_workers floor and the max_workers ceiling bill per hour only while task queue depth demands them
        - autoscaled web servers between the min_webservers floor and the max_webservers ceiling bill per hour only while UI and REST-API load demands them
        - metadata database storage bills per GB-month as workflow history accumulates
        - CloudWatch Logs ingestion and storage for the enabled log modules depend on log level and DAG volume
        - the DAG source bucket, execution role, and KMS key bill on their own components
      notes: Estimated at the scaling floors -- the committed always-on spend; the environment fee already includes one worker, two schedulers, and two web servers.
