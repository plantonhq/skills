# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file joins the component's estimate model (../models/, the authored
# quantities with their derivations) with the provider's price book
# (../pricebook/, pinned unit prices with source URL and retrieval date):
# every line cost and preset total is computed exactly, lines are ordered
# largest cost first, and the catalog-data conformance gate re-verifies all
# arithmetic. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azurestoragecontainer
spec:
  presets:
    - preset: 01-private-container
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - blob data stored bills per GB-month on the parent account at its tier-and-redundancy rate (50 TB / 500 TB price breaks) as blobs arrive -- volume no manifest can know
        - write, read, list-and-create-container, and all-other operations each bill per 10,000 on the account (delete is free); cool, cold, and archive reads add per-GB data retrieval and early-deletion penalties apply below minimum retention
        - blob index tags bill per 10,000 tags per month on tagged blobs; soft-deleted data bills at the active-data rate for the account's retention window
        - account-level features the data participates in (versioning capacity, change feed per 10,000 changes, inventory, lifecycle transitions, SFTP hours) and geo-replication or internet egress bill on the account
      notes: Zero committed cost -- a private container is a free namespace; an empty one holds for exactly nothing, and every future dollar bills on the account's meters at the account's tier and redundancy.
    - preset: 02-public-cdn-origin
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - anonymous BLOB access carries no meter of its own, but every anonymous GET bills the account's read operations per 10,000 plus internet egress per GB -- a public origin lets the whole internet generate metered traffic
        - blob data stored bills per GB-month on the account; write, list-and-create-container, and all-other operations bill per 10,000
        - blob index tags, soft-delete retention capacity, versioning, change feed, inventory, and lifecycle transitions bill on the account when enabled
        - a CDN or Front Door fronting this origin bills on its own component
      notes: Zero committed cost -- the BLOB access posture is a free spec choice; it changes who can create spend (anyone, anonymously), never the rates or the container's own nonexistent meter.
    - preset: 03-tenant-scoped-encryption
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - the referenced AzureStorageEncryptionScope bills a flat monthly per-scope fee (with a 30-day minimum per enablement) on ITS component from the moment it is enabled -- binding it here adds nothing to the container's bill
        - a Key Vault-keyed scope adds per-transaction wrap/unwrap operations on the key's vault, billed to the vault
        - blob data stored per GB-month and the per-10,000 operation meters bill on the account exactly as in the private preset; blob index tags, soft-delete capacity, and account-level feature meters apply when used
      notes: Zero committed cost FOR THIS COMPONENT -- the tenant-isolation pattern's committed spend (the scope's monthly fee) rides the referenced encryption-scope component, and the container remains a free namespace binding it.
