# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awselasticip
spec:
  presets:
    - preset: 01-standard-eip
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Virtual Private Cloud
          sku_meter: public IPv4 address usage
          pricing_unit: IP-hours
          pricing_quantity: "730"
          quantity_basis: one address with no association target in this manifest, metered idle every hour of the month; the idle rate equals the in-use rate, so a consumer attaching the address elsewhere (an NLB subnet mapping, a NAT gateway) changes the meter name, not the number
          list_unit_price: "0.005"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonVPC/20260724154225/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "3.6500"
      total_list_cost: "3.6500"
      exclusions:
        - Elastic IP remaps beyond AWS's free monthly allowance bill per remap -- a usage charge driven by how often automation re-associates the address, invisible to this manifest.
        - The associated resource (instance, network interface, NAT gateway, load balancer) and all data transfer through the address bill on their own meters, outside this component.
      notes: The address-hours are the committed bill, and releasing the address is the only way to stop the meter.
    - preset: 02-byoip-pool
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - Elastic IP remaps beyond AWS's free monthly allowance bill per remap -- a usage charge driven by how often automation re-associates the address, invisible to this manifest.
        - The associated resource (instance, network interface, NAT gateway, load balancer) and all data transfer through the address bill on their own meters, outside this component.
      notes: The address-hours are the committed bill, and releasing the address is the only way to stop the meter. A BYOIP address is not charged the public IPv4 rate -- AWS bills only addresses from its own space -- so this manifest's committed monthly cost is honestly zero.
    - preset: 03-instance-attached
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Virtual Private Cloud
          sku_meter: public IPv4 address usage
          pricing_unit: IP-hours
          pricing_quantity: "730"
          quantity_basis: one address associated with an instance, metered in-use every hour of the month; every catalog preset leaves region a placeholder, so this prices the us-east-1 rate
          list_unit_price: "0.005"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonVPC/20260724154225/us-east-1/index.json
          retrieved_on: "2026-08-14"
          list_cost: "3.6500"
      total_list_cost: "3.6500"
      exclusions:
        - Elastic IP remaps beyond AWS's free monthly allowance bill per remap -- a usage charge driven by how often automation re-associates the address, invisible to this manifest.
        - The associated resource (instance, network interface, NAT gateway, load balancer) and all data transfer through the address bill on their own meters, outside this component.
      notes: The address-hours are the committed bill, and releasing the address is the only way to stop the meter.
