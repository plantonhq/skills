# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azureeventhubcluster
spec:
  presets:
    - preset: 01-dedicated-cluster
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Event Hubs
          sku_meter: dedicated capacity units
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: capacity_units (Azure's entry size 1 when unset -- the modules compose the ARM sku "Dedicated_{n}" with the same default) x 730 hours at the Dedicated Capacity Unit rate
          list_unit_price: "6.849"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Event Hubs' and armRegionName eq 'eastus' and meterName eq 'Dedicated Capacity Unit' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-16"
          list_cost: "4999.7700"
      total_list_cost: "4999.7700"
      exclusions:
        - event data retained beyond the included window bills per GB-month on the Dedicated Extended Retention meter, following event volume and per-hub retention settings
        - network egress follows consumer placement
      notes: The CU-hours are the whole committed bill -- namespaces placed on the cluster draw on its capacity instead of carrying their own throughput meters. Azure forbids deleting a cluster for 4 hours after creation, so even the shortest-lived cluster pays at least four CU-hours.
