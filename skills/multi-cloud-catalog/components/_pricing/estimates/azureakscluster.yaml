# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azureakscluster
spec:
  presets:
    - preset: 01-standard
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual Machines
          sku_meter: node pool VM hours
          pricing_unit: hours
          pricing_quantity: "2190"
          quantity_basis: default pool at the autoscaling floor (min_count) x 730 hours, Linux pay-as-you-go
          list_unit_price: "0.192"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual Machines' and armRegionName eq 'eastus' and armSkuName eq 'Standard_D4s_v5' and meterName eq 'D4s v5' and productName eq 'Virtual Machines Dsv5 Series' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "420.4800"
        - service_name: Azure Kubernetes Service
          sku_meter: cluster management fee
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: sku_tier STANDARD bills the uptime-SLA control plane every hour of the month
          list_unit_price: "0.1"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Azure Kubernetes Service' and meterName eq 'Standard Uptime SLA' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "73.0000"
        - service_name: Storage
          sku_meter: node OS disk storage
          pricing_unit: disk-months
          pricing_quantity: "3"
          quantity_basis: one P10 (128 GiB) Premium SSD managed OS disk per node at the autoscaling floor -- the AKS default size when os_disk_size_gb is unset
          list_unit_price: "19.71"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Storage' and armRegionName eq 'eastus' and meterName eq 'P10 LRS Disk' and productName eq 'Premium SSD Managed Disks' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "59.1300"
        - service_name: Load Balancer
          sku_meter: included load balancer rules
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: the default outbound_type (LOAD_BALANCER) provisions a Standard Load Balancer whose included-rules bundle bills every hour
          list_unit_price: "0.025"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Load Balancer' and armRegionName eq 'Global' and meterName eq 'Standard Included LB Rules and Outbound Rules' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "18.2500"
        - service_name: Virtual Network
          sku_meter: standard static public IP
          pricing_unit: IP-hours
          pricing_quantity: "730"
          quantity_basis: the managed outbound public IPs (Azure's default is one) x 730 hours
          list_unit_price: "0.005"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual Network' and armRegionName eq 'Global' and meterName eq 'Standard IPv4 Static Public IP' and productName eq 'IP Addresses' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "3.6500"
      total_list_cost: "574.5100"
      exclusions:
        - Log Analytics ingestion for Container Insights (oms_agent) depends on workload log volume
        - autoscaled nodes above the floor are workload-dependent (max_count sets the VM ceiling)
        - load balancer data processed and network egress are traffic-dependent
      notes: Estimated at the autoscaling floor -- the committed always-on spend.
    - preset: 02-private
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual Machines
          sku_meter: node pool VM hours
          pricing_unit: hours
          pricing_quantity: "2190"
          quantity_basis: default pool at the autoscaling floor (min_count) x 730 hours, Linux pay-as-you-go
          list_unit_price: "0.192"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual Machines' and armRegionName eq 'eastus' and armSkuName eq 'Standard_D4s_v5' and meterName eq 'D4s v5' and productName eq 'Virtual Machines Dsv5 Series' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "420.4800"
        - service_name: Azure Kubernetes Service
          sku_meter: cluster management fee
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: sku_tier STANDARD bills the uptime-SLA control plane every hour of the month
          list_unit_price: "0.1"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Azure Kubernetes Service' and meterName eq 'Standard Uptime SLA' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "73.0000"
        - service_name: Storage
          sku_meter: node OS disk storage
          pricing_unit: disk-months
          pricing_quantity: "3"
          quantity_basis: one P10 (128 GiB) Premium SSD managed OS disk per node at the autoscaling floor -- the AKS default size when os_disk_size_gb is unset
          list_unit_price: "19.71"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Storage' and armRegionName eq 'eastus' and meterName eq 'P10 LRS Disk' and productName eq 'Premium SSD Managed Disks' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "59.1300"
        - service_name: Load Balancer
          sku_meter: included load balancer rules
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: the default outbound_type (LOAD_BALANCER) provisions a Standard Load Balancer whose included-rules bundle bills every hour
          list_unit_price: "0.025"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Load Balancer' and armRegionName eq 'Global' and meterName eq 'Standard Included LB Rules and Outbound Rules' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "18.2500"
        - service_name: Virtual Network
          sku_meter: standard static public IP
          pricing_unit: IP-hours
          pricing_quantity: "730"
          quantity_basis: the managed outbound public IPs (Azure's default is one) x 730 hours
          list_unit_price: "0.005"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual Network' and armRegionName eq 'Global' and meterName eq 'Standard IPv4 Static Public IP' and productName eq 'IP Addresses' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "3.6500"
      total_list_cost: "574.5100"
      exclusions:
        - private endpoint and private DNS zone charges for the private API server
        - autoscaled nodes above the floor are workload-dependent (max_count sets the VM ceiling)
        - load balancer data processed and network egress are traffic-dependent
      notes: Estimated at the autoscaling floor -- the committed always-on spend.
    - preset: 03-hardened-enterprise
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual Machines
          sku_meter: node pool VM hours
          pricing_unit: hours
          pricing_quantity: "2190"
          quantity_basis: default pool at the autoscaling floor (min_count) x 730 hours, Linux pay-as-you-go
          list_unit_price: "0.192"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual Machines' and armRegionName eq 'eastus' and armSkuName eq 'Standard_D4s_v5' and meterName eq 'D4s v5' and productName eq 'Virtual Machines Dsv5 Series' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "420.4800"
        - service_name: Azure Kubernetes Service
          sku_meter: cluster management fee
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: sku_tier STANDARD bills the uptime-SLA control plane every hour of the month
          list_unit_price: "0.1"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Azure Kubernetes Service' and meterName eq 'Standard Uptime SLA' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "73.0000"
        - service_name: Storage
          sku_meter: node OS disk storage
          pricing_unit: disk-months
          pricing_quantity: "3"
          quantity_basis: one P10 (128 GiB) Premium SSD managed OS disk per node at the autoscaling floor -- the AKS default size when os_disk_size_gb is unset
          list_unit_price: "19.71"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Storage' and armRegionName eq 'eastus' and meterName eq 'P10 LRS Disk' and productName eq 'Premium SSD Managed Disks' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "59.1300"
        - service_name: Load Balancer
          sku_meter: included load balancer rules
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: the default outbound_type (LOAD_BALANCER) provisions a Standard Load Balancer whose included-rules bundle bills every hour
          list_unit_price: "0.025"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Load Balancer' and armRegionName eq 'Global' and meterName eq 'Standard Included LB Rules and Outbound Rules' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "18.2500"
        - service_name: Virtual Network
          sku_meter: standard static public IP
          pricing_unit: IP-hours
          pricing_quantity: "730"
          quantity_basis: the managed outbound public IPs (Azure's default is one) x 730 hours
          list_unit_price: "0.005"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual Network' and armRegionName eq 'Global' and meterName eq 'Standard IPv4 Static Public IP' and productName eq 'IP Addresses' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-14"
          list_cost: "3.6500"
      total_list_cost: "574.5100"
      exclusions:
        - Log Analytics ingestion for Container Insights (oms_agent) depends on workload log volume
        - Microsoft Defender for Containers plan charges (billed per protected vCPU per month)
        - autoscaled nodes above the floor are workload-dependent (max_count sets the VM ceiling)
        - load balancer data processed and network egress are traffic-dependent
      notes: Estimated at the autoscaling floor -- the committed always-on spend. Host encryption on the default pool adds no charge of its own.
