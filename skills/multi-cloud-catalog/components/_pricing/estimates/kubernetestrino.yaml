# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its capacity derivation
# (../capacity/, the machine-executable value-to-capacity rules): each
# preset states the CPU/memory requests and limits and the persistent
# volume storage it reserves from its target cluster, with a basis sentence
# tracing every total back to the manifest's values. Cluster-capacity
# components carry no priced lines -- the dollar value of the reservation
# is the target cluster's node and storage-class economics, which this
# catalog cannot know. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: kubernetestrino
spec:
  presets:
    - preset: 02-federated-warehouse
      capacity_footprint:
        cpu_requests: 2500m
        memory_requests: 10Gi
        memory_limits: 16Gi
        basis: 1 coordinator x (500m CPU / 2Gi memory requests, 4Gi limits); 2 workers x (1 CPU / 4Gi memory requests, 6Gi limits)
      exclusions:
        - the dollar value of this capacity is the target cluster's node and storage-class economics
        - the worker HPA owns the worker count at runtime up to its declared max -- the replicas field is ignored while the arm is set, so the footprint states the declared (or default) replicas
        - the Prometheus JMX exporter sidecar rides EVERY Trino pod at chart-default sizing -- the spec models no resources for it
        - fault-tolerant exchange spooling writes to external storage (object stores or local paths) and the catalog data sources are separate components -- neither rides this footprint
      notes: Trino is diskless by design -- it provisions no volumes and queries data where it lives, so the footprint is purely compute reservations workers.replicas 0 with coordinator.include_in_scheduling gives the single-node shape -- the worker binding then contributes nothing and the coordinator carries all execution
