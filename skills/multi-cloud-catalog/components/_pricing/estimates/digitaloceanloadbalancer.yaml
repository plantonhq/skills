# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: digitaloceanloadbalancer
spec:
  presets:
    - preset: 01-https-ssl-termination
      region_assumption: nyc3
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Load Balancers
          sku_meter: load balancer nodes
          pricing_unit: months
          pricing_quantity: "1"
          quantity_basis: one node -- the lb-small shape, whether pinned explicitly or taken as DigitalOcean's default when both size and size_unit are unset
          list_unit_price: "12.00"
          price_source: https://www.digitalocean.com/pricing/load-balancers
          retrieved_on: "2026-08-22"
          list_cost: "12.0000"
      total_list_cost: "12.0000"
      exclusions:
        - backend droplets bill on their own DigitalOceanDroplet components; attachment adds no charge
        - traffic through the balancer carries no separate bandwidth meter -- outbound transfer draws from the backend droplets' pooled monthly allowance
      notes: Priced at DigitalOcean's global regional-balancer rate of 12.00 USD per node per month -- the pricing page carries no region dimension. Forwarding rules, health checks, sticky sessions, the firewall, and TLS termination are free configuration on the same nodes.
    - preset: 02-http-basic
      region_assumption: nyc3
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Load Balancers
          sku_meter: load balancer nodes
          pricing_unit: months
          pricing_quantity: "1"
          quantity_basis: one node -- the lb-small shape, whether pinned explicitly or taken as DigitalOcean's default when both size and size_unit are unset
          list_unit_price: "12.00"
          price_source: https://www.digitalocean.com/pricing/load-balancers
          retrieved_on: "2026-08-22"
          list_cost: "12.0000"
      total_list_cost: "12.0000"
      exclusions:
        - backend droplets bill on their own DigitalOceanDroplet components; attachment adds no charge
        - traffic through the balancer carries no separate bandwidth meter -- outbound transfer draws from the backend droplets' pooled monthly allowance
      notes: Priced at DigitalOcean's global regional-balancer rate of 12.00 USD per node per month -- the pricing page carries no region dimension. Forwarding rules, health checks, sticky sessions, the firewall, and TLS termination are free configuration on the same nodes.
