# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azurefabriccapacity
spec:
  presets:
    - preset: 01-starter-capacity
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Microsoft Fabric
          sku_meter: capacity compute
          pricing_unit: CU-hours
          pricing_quantity: "1460"
          quantity_basis: F2 provisions 2 Capacity Units x 730 hours at the pay-as-you-go CU-hour rate
          list_unit_price: "0.18"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Microsoft Fabric' and armRegionName eq 'eastus' and armSkuName eq 'Compute Pool' and meterName eq 'Compute Pool Capacity Usage CU' and productName eq 'Fabric Capacity' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-16"
          list_cost: "262.8000"
      total_list_cost: "262.8000"
      exclusions:
        - OneLake storage bills per GB-month on its own meters ('OneLake Storage Hot Data Stored' and its cool/cold tiers), following data volume
        - capacity overage consumption, where enabled, bills its own CU meter ('Capacity Overage Capacity Usage CU') beyond the provisioned CUs
      notes: Estimated always-on -- a suspended capacity stops the CU-hour meter entirely, so 730 hours is the never-paused ceiling.
