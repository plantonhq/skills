# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azurepointtositevpngateway
spec:
  presets:
    - preset: 01-standard-remote-users
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual WAN
          sku_meter: VPN P2S scale units
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: the gateway bills its scale units (capacity across the managed instance pair; unset applies the provider's default of 1) x 730 hours at the flat per-unit rate, connected users or none
          list_unit_price: "0.361"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual WAN' and armRegionName eq 'eastus' and meterName eq 'VPN P2S Scale Unit' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "263.5300"
      total_list_cost: "263.5300"
      exclusions:
        - connection units bill per concurrently connected remote user per hour -- attendance the manifest cannot know; the address pools bound it without fixing it
        - data leaving Azure through client tunnels bills at standard data-transfer rates and follows traffic
        - the hub's deployment hours and per-GB data processing bill on the AzureVirtualHub component
    - preset: 02-forced-tunnel-clients
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual WAN
          sku_meter: VPN P2S scale units
          pricing_unit: hours
          pricing_quantity: "1460"
          quantity_basis: the gateway bills its scale units (capacity across the managed instance pair; unset applies the provider's default of 1) x 730 hours at the flat per-unit rate, connected users or none
          list_unit_price: "0.361"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual WAN' and armRegionName eq 'eastus' and meterName eq 'VPN P2S Scale Unit' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "527.0600"
      total_list_cost: "527.0600"
      exclusions:
        - connection units bill per concurrently connected remote user per hour -- attendance the manifest cannot know; the address pools bound it without fixing it
        - data leaving Azure through client tunnels bills at standard data-transfer rates and follows traffic
        - the hub's deployment hours and per-GB data processing bill on the AzureVirtualHub component
