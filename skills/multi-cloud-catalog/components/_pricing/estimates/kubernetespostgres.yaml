# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its capacity derivation
# (../capacity/, the machine-executable value-to-capacity rules): each
# preset states the CPU/memory requests and limits and the persistent
# volume storage it reserves from its target cluster, with a basis sentence
# tracing every total back to the manifest's values. Cluster-capacity
# components carry no priced lines -- the dollar value of the reservation
# is the target cluster's node and storage-class economics, which this
# catalog cannot know. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: kubernetespostgres
spec:
  presets:
    - preset: 01-dev-single-instance
      capacity_footprint:
        cpu_requests: 100m
        memory_requests: 256Mi
        cpu_limits: 500m
        memory_limits: 512Mi
        persistent_storage: 5Gi
        basis: 1 instance x (100m CPU / 256Mi memory requests, 500m CPU / 512Mi limits) + 1 x 5Gi data volume
      exclusions:
        - the dollar value of this capacity is the target cluster's node and storage-class economics
    - preset: 02-production-ha
      capacity_footprint:
        cpu_requests: "3"
        memory_requests: 6Gi
        cpu_limits: "6"
        memory_limits: 12Gi
        persistent_storage: 360Gi
        basis: 3 instances x (1 CPU / 2Gi memory requests, 2 CPU / 4Gi limits) + 3 x (100Gi data + 20Gi WAL) volumes
      exclusions:
        - the dollar value of this capacity is the target cluster's node and storage-class economics
        - object storage for the base backups and WAL archive grows with database churn across the retention window
    - preset: 03-s3-compatible-backups
      capacity_footprint:
        cpu_requests: 1500m
        memory_requests: 3Gi
        cpu_limits: "6"
        memory_limits: 6Gi
        persistent_storage: 150Gi
        basis: 3 instances x (500m CPU / 1Gi memory requests, 2 CPU / 2Gi limits) + 3 x 50Gi data volumes
      exclusions:
        - the dollar value of this capacity is the target cluster's node and storage-class economics
        - object storage on the configured S3-compatible endpoint for the base backups and WAL archive grows with database churn across the retention window
