# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: digitaloceandroplet
spec:
  presets:
    - preset: 01-production
      region_assumption: nyc3
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Droplets
          sku_meter: droplet instance
          pricing_unit: months
          pricing_quantity: "1"
          quantity_basis: one droplet of the manifest's size existing the whole month -- per-second billing caps at the size's flat monthly price, so the cap is the committed monthly charge
          list_unit_price: "24.00"
          price_source: https://www.digitalocean.com/pricing/droplets
          retrieved_on: "2026-08-22"
          list_cost: "24.0000"
        - service_name: Droplets
          sku_meter: automated backups
          pricing_unit: months
          pricing_quantity: "1"
          quantity_basis: weekly automated backups bill 20 percent of the droplet's monthly cost while enabled -- the book entry pins that surcharge for this size
          list_unit_price: "4.80"
          price_source: https://www.digitalocean.com/pricing/droplets
          retrieved_on: "2026-08-22"
          list_cost: "4.8000"
      total_list_cost: "28.8000"
      exclusions:
        - outbound data transfer beyond the size's included monthly allowance bills per GiB; inbound is free
        - droplet snapshots bill 0.06 USD/GB per month and are taken outside this spec
      notes: Priced at DigitalOcean's global droplet rates -- the pricing page carries one price table with no region dimension, so the resolved region moves availability, not the rate. The estimate assumes the droplet exists all 730 hours; per-second billing means a short-lived droplet bills less, never more, than this cap.
    - preset: 02-development
      region_assumption: digital_ocean_region_unspecified
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Droplets
          sku_meter: droplet instance
          pricing_unit: months
          pricing_quantity: "1"
          quantity_basis: one droplet of the manifest's size existing the whole month -- per-second billing caps at the size's flat monthly price, so the cap is the committed monthly charge
          list_unit_price: "6.00"
          price_source: https://www.digitalocean.com/pricing/droplets
          retrieved_on: "2026-08-22"
          list_cost: "6.0000"
      total_list_cost: "6.0000"
      exclusions:
        - outbound data transfer beyond the size's included monthly allowance bills per GiB; inbound is free
        - droplet snapshots bill 0.06 USD/GB per month and are taken outside this spec
      notes: Priced at DigitalOcean's global droplet rates -- the pricing page carries one price table with no region dimension, so the resolved region moves availability, not the rate. The estimate assumes the droplet exists all 730 hours; per-second billing means a short-lived droplet bills less, never more, than this cap.
