# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: gcpgkecluster
spec:
  presets:
    - preset: 01-private-standard
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Google Kubernetes Engine
          sku_meter: cluster management fee
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one cluster, billed every hour of the month at the flat per-cluster rate (identical for zonal and regional clusters)
          list_unit_price: "0.1"
          price_source: https://cloudbilling.googleapis.com/v1/services/CCD8-9BF1-090E/skus
          retrieved_on: "2026-08-14"
          list_cost: "73.0000"
      total_list_cost: "73.0000"
      exclusions:
        - node compute -- this Standard-mode cluster runs its workloads on separate GcpGkeNodePool resources with their own estimates
        - Cloud Logging ingestion for the enabled log components, network egress, and Cloud NAT for private nodes
      notes: Zonal clusters share the GKE free-tier credit; a REGIONAL cluster never receives it and pays the management fee in full. List total shown without any credit.
    - preset: 02-autopilot
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Google Kubernetes Engine
          sku_meter: cluster management fee
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one cluster, billed every hour of the month at the flat per-cluster rate (identical for zonal and regional clusters)
          list_unit_price: "0.1"
          price_source: https://cloudbilling.googleapis.com/v1/services/CCD8-9BF1-090E/skus
          retrieved_on: "2026-08-14"
          list_cost: "73.0000"
      total_list_cost: "73.0000"
      exclusions:
        - Autopilot pod charges -- running pods bill per second for the vCPU, memory, and ephemeral storage they request, entirely workload-dependent
        - Cloud Logging ingestion for the enabled log components, network egress, and Cloud NAT for private nodes
      notes: The GKE free tier credits $74.40 per month per billing account and applies to Autopilot clusters -- the account's first Autopilot or zonal cluster effectively pays no management fee. List total shown without the credit.
    - preset: 03-dev-zonal
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Google Kubernetes Engine
          sku_meter: cluster management fee
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one cluster, billed every hour of the month at the flat per-cluster rate (identical for zonal and regional clusters)
          list_unit_price: "0.1"
          price_source: https://cloudbilling.googleapis.com/v1/services/CCD8-9BF1-090E/skus
          retrieved_on: "2026-08-14"
          list_cost: "73.0000"
      total_list_cost: "73.0000"
      exclusions:
        - node compute -- this Standard-mode cluster runs its workloads on separate GcpGkeNodePool resources with their own estimates
        - Cloud Logging ingestion for the enabled log components, network egress, and Cloud NAT for private nodes
      notes: Zonal clusters share the GKE free-tier credit; a REGIONAL cluster never receives it and pays the management fee in full. List total shown without any credit.
