# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azurevirtualhub
spec:
  presets:
    - preset: 01-standard-hub
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual WAN
          sku_meter: Standard hub deployment
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one Standard hub x 730 hours at the Standard Hub Unit rate -- this arm covers sku STANDARD and an unspecified sku, which deploys STANDARD (ARM's default)
          list_unit_price: "0.25"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual WAN' and armRegionName eq 'eastus' and meterName eq 'Standard Hub Unit' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "182.5000"
      total_list_cost: "182.5000"
      exclusions:
        - hub data processing bills per GB for VNet-to-VNet traffic transiting the router, and the router autoscales -- and bills -- routing infrastructure units above the included 2 when traffic demands it
        - gateways created on the hub (site-to-site VPN, point-to-site VPN, ExpressRoute) are their own kinds billing scale units and connection units
        - hub-to-hub and inter-region flows bill standard inter-region data-transfer rates
      notes: Custom route tables, static routes, and BGP peerings with NVAs carry no meters of their own -- the price list publishes none.
    - preset: 02-isolated-spokes-hub
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual WAN
          sku_meter: Standard hub deployment
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one Standard hub x 730 hours at the Standard Hub Unit rate -- this arm covers sku STANDARD and an unspecified sku, which deploys STANDARD (ARM's default)
          list_unit_price: "0.25"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual WAN' and armRegionName eq 'eastus' and meterName eq 'Standard Hub Unit' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "182.5000"
      total_list_cost: "182.5000"
      exclusions:
        - hub data processing bills per GB for VNet-to-VNet traffic transiting the router, and the router autoscales -- and bills -- routing infrastructure units above the included 2 when traffic demands it
        - gateways created on the hub (site-to-site VPN, point-to-site VPN, ExpressRoute) are their own kinds billing scale units and connection units
        - hub-to-hub and inter-region flows bill standard inter-region data-transfer rates
      notes: Custom route tables, static routes, and BGP peerings with NVAs carry no meters of their own -- the price list publishes none.
    - preset: 03-secured-hub-routing-intent
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual WAN
          sku_meter: Standard hub deployment
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one Standard hub x 730 hours at the Standard Hub Unit rate -- this arm covers sku STANDARD and an unspecified sku, which deploys STANDARD (ARM's default)
          list_unit_price: "0.25"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual WAN' and armRegionName eq 'eastus' and meterName eq 'Standard Hub Unit' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "182.5000"
      total_list_cost: "182.5000"
      exclusions:
        - hub data processing bills per GB for VNet-to-VNet traffic transiting the router, and the router autoscales -- and bills -- routing infrastructure units above the included 2 when traffic demands it
        - gateways created on the hub (site-to-site VPN, point-to-site VPN, ExpressRoute) are their own kinds billing scale units and connection units
        - the Azure Firewall the routing intent steers traffic through bills its secured-hub deployment and data-processing meters on its own component
        - hub-to-hub and inter-region flows bill standard inter-region data-transfer rates
      notes: Custom route tables, static routes, and BGP peerings with NVAs carry no meters of their own -- the price list publishes none.
