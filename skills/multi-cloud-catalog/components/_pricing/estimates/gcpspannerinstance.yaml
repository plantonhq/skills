# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: gcpspannerinstance
spec:
  presets:
    - preset: 01-free-instance
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - database storage bills per GiB-month of data actually stored across the configuration's replicas and grows with the databases
        - network egress and Data Boost usage are workload-dependent
      notes: A FREE_INSTANCE is the billing account's one zero-cost development instance (about 10 GB of storage, no capacity fields) -- the total is genuinely zero, not an omission. Google frames the free instance as a 90-day trial; at expiry it must upgrade to PROVISIONED (starting normal billing) or be reclaimed.
    - preset: 02-regional-production
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Cloud Spanner
          sku_meter: compute capacity
          pricing_unit: replica-hours
          pricing_quantity: "2190"
          quantity_basis: num_nodes nodes (GCP defaults a PROVISIONED instance to 1 node when no capacity field is set), each running the regional configuration's three read-write replicas, billed every hour at the edition's per-replica rate -- a manifest that names no edition refuses because the book pins edition-keyed rates
          list_unit_price: "0.41"
          price_source: https://cloudbilling.googleapis.com/v1/services/CC63-0873-48FD/skus
          retrieved_on: "2026-08-16"
          list_cost: "897.9000"
      total_list_cost: "897.9000"
      exclusions:
        - database storage bills per GiB-month of data actually stored across the configuration's replicas and grows with the databases
        - backup storage bills per GiB-month -- AUTOMATIC gives every new database on the instance a default backup schedule, so backup GiB start accruing with the first database
        - network egress and Data Boost usage are workload-dependent
      notes: Spanner's published regional node price is the per-replica rate times three read-write replicas -- the quantity carries the replicas so the rate stays the billing SKU's own number.
