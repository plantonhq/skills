# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awscloudwatchalarm
spec:
  presets:
    - preset: 01-cpu-utilization-alarm
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: AmazonCloudWatch
          sku_meter: alarm metrics
          pricing_unit: alarm-metric-months
          pricing_quantity: "1"
          quantity_basis: one simple-metric alarm watches exactly one metric and bills one flat alarm-metric month at the standard-resolution rate -- the spec's period validation makes any non-sub-minute period a multiple of 60; priced at the us-east-1 list rate, the assumed region when the manifest leaves region unset
          list_unit_price: "0.1"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonCloudWatch/20260806201840/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "0.1000"
      total_list_cost: "0.1000"
      exclusions:
        - alarm, OK, and insufficient-data actions bill on their targets -- SNS topics referenced by the action fields carry their own AwsSnsTopic cost
      notes: The alarm fee is flat and committed at creation -- evaluation_periods, datapoints_to_alarm, and how often the alarm fires never move it.
    - preset: 03-production-multi-action
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: AmazonCloudWatch
          sku_meter: alarm metrics
          pricing_unit: alarm-metric-months
          pricing_quantity: "1"
          quantity_basis: one simple-metric alarm watches exactly one metric and bills one flat alarm-metric month at the standard-resolution rate -- the spec's period validation makes any non-sub-minute period a multiple of 60; priced at the us-east-1 list rate, the assumed region when the manifest leaves region unset
          list_unit_price: "0.1"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonCloudWatch/20260806201840/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "0.1000"
      total_list_cost: "0.1000"
      exclusions:
        - alarm, OK, and insufficient-data actions bill on their targets -- SNS topics referenced by the action fields carry their own AwsSnsTopic cost
      notes: The alarm fee is flat and committed at creation -- evaluation_periods, datapoints_to_alarm, and how often the alarm fires never move it.
