# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file joins the component's estimate model (../models/, the authored
# quantities with their derivations) with the provider's price book
# (../pricebook/, pinned unit prices with source URL and retrieval date):
# every line cost and preset total is computed exactly, lines are ordered
# largest cost first, and the catalog-data conformance gate re-verifies all
# arithmetic. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azuremachinelearningbatchdeployment
spec:
  presets:
    - preset: 01-model-scoring-recipe
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - job node-hours bill on the referenced compute cluster component (its dedicated or low-priority VM meters) for each invocation's duration -- this recipe spreads each job across 4 nodes, so a one-hour scoring run is four node-hours on that pool, and zero invocations is zero node-hours
        - retries extend the billed hours -- up to 3 per failed mini-batch here, capped job-wide by errorThreshold 100
        - job inputs and the AppendRow predictions output rest in workspace datastores, billing storage capacity and transactions on the storage account's own component
        - network egress follows what jobs move
      notes: Zero committed cost -- the recipe object carries no meter under Azure Machine Learning's no-additional-charge model; an idle deployment on a scale-to-zero pool bills nothing at all.
    - preset: 02-pipeline-component-recipe
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - pipeline jobs' node-hours bill on whatever compute pools the settings name (default_compute here) -- each step's runtime lands on that pool's own component at plain VM rates, only while the pipeline runs
        - the pipeline's intermediate and final outputs rest in workspace datastores, billing storage on the storage account's own component
        - network egress follows what pipeline steps move
      notes: Zero committed cost -- a pipeline-component recipe is a stable trigger address for a whole batch pipeline, and holding the trigger costs nothing; spend materializes per invocation on the pools the pipeline names.
