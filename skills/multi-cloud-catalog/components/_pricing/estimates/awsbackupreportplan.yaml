# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file joins the component's estimate model (../models/, the authored
# quantities with their derivations) with the provider's price book
# (../pricebook/, pinned unit prices with source URL and retrieval date):
# every line cost and preset total is computed exactly, lines are ordered
# largest cost first, and the catalog-data conformance gate re-verifies all
# arithmetic. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awsbackupreportplan
spec:
  presets:
    - preset: 01-daily-job-report
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - report generation and delivery are uncharged by the Backup service -- the daily job report itself carries no meter
        - requesting both CSV and JSON delivers two files per daily run, and the files accumulate in the referenced bucket billing S3 storage and PUT requests on the AwsS3Bucket component until a bucket-side lifecycle rule expires them
        - report file size scales with fleet activity -- a busier backup fleet writes bigger daily files
      notes: Zero committed cost at the Backup service -- the quietly accumulating spend is S3-side, roughly sixty small files a month (two formats, daily) whose retention is entirely the bucket's problem.
    - preset: 02-compliance-evidence
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - the compliance report itself is uncharged -- but it reports on Audit Manager framework evaluations, which bill per evaluation (plus AWS Config configuration items) on the referenced AwsBackupFramework component, not here
        - delivered report files bill S3 storage and requests on the referenced bucket component, accumulating daily with no pruning in this spec
      notes: Zero committed cost -- the evidence trail is free to produce; the spend that makes it worth producing (the framework's evaluations) bills on the framework being reported on.
