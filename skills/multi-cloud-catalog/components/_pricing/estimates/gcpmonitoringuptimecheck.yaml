# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: gcpmonitoringuptimecheck
spec:
  presets:
    - preset: 01-public-https-check
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Cloud Monitoring
          sku_meter: uptime check executions
          pricing_unit: executions
          pricing_quantity: "52560"
          quantity_basis: six checker locations (the empty selected_regions default) x 12 probes per hour at the 300s cadence x 730 hours, at the per-execution list rate
          list_unit_price: "0.0003"
          price_source: https://cloudbilling.googleapis.com/v1/services/58CD-E7C3-72CA/skus
          retrieved_on: "2026-08-16"
          list_cost: "15.7680"
      total_list_cost: "15.7680"
      exclusions:
        - the alerting built on this check (alert policies and their notification channels) bills on those components
      notes: The first 1,000,000 uptime check executions per project each month are free -- the SKU's own tiered rate, aggregated monthly at project level -- so a project's early checks pay nothing and this may be one of them. List total shown without the free allotment, which is project-wide and not visible from any single manifest.
    - preset: 02-tcp-port-check
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Cloud Monitoring
          sku_meter: uptime check executions
          pricing_unit: executions
          pricing_quantity: "262800"
          quantity_basis: six checker locations (the empty selected_regions default) x 60 probes per hour at the 60s cadence x 730 hours, at the per-execution list rate
          list_unit_price: "0.0003"
          price_source: https://cloudbilling.googleapis.com/v1/services/58CD-E7C3-72CA/skus
          retrieved_on: "2026-08-16"
          list_cost: "78.8400"
      total_list_cost: "78.8400"
      exclusions:
        - the alerting built on this check (alert policies and their notification channels) bills on those components
      notes: The first 1,000,000 uptime check executions per project each month are free -- the SKU's own tiered rate, aggregated monthly at project level -- so a project's early checks pay nothing and this may be one of them. List total shown without the free allotment, which is project-wide and not visible from any single manifest.
    - preset: 03-authenticated-api-check
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Cloud Monitoring
          sku_meter: uptime check executions
          pricing_unit: executions
          pricing_quantity: "52560"
          quantity_basis: six checker locations (the empty selected_regions default) x 12 probes per hour at the 300s cadence x 730 hours, at the per-execution list rate
          list_unit_price: "0.0003"
          price_source: https://cloudbilling.googleapis.com/v1/services/58CD-E7C3-72CA/skus
          retrieved_on: "2026-08-16"
          list_cost: "15.7680"
      total_list_cost: "15.7680"
      exclusions:
        - the alerting built on this check (alert policies and their notification channels) bills on those components
      notes: The first 1,000,000 uptime check executions per project each month are free -- the SKU's own tiered rate, aggregated monthly at project level -- so a project's early checks pay nothing and this may be one of them. List total shown without the free allotment, which is project-wide and not visible from any single manifest.
