# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its capacity derivation
# (../capacity/, the machine-executable value-to-capacity rules): each
# preset states the CPU/memory requests and limits and the persistent
# volume storage it reserves from its target cluster, with a basis sentence
# tracing every total back to the manifest's values. Cluster-capacity
# components carry no priced lines -- the dollar value of the reservation
# is the target cluster's node and storage-class economics, which this
# catalog cannot know. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: kuberneteskafkamirrormaker2
spec:
  presets:
    - preset: 01-migrate-from-msk
      capacity_footprint:
        cpu_requests: 1500m
        memory_requests: 6Gi
        cpu_limits: "6"
        memory_limits: 12Gi
        basis: 3 workers x (500m CPU / 2Gi memory requests, 2 CPU / 4Gi limits)
      exclusions:
        - the dollar value of this capacity is the target cluster's node economics
        - the engine's internal storage topics (configs, offsets, status) live on the TARGET Kafka cluster and grow there
        - mirrored topic data lands on the target Kafka cluster and grows with source traffic across the mirroring window
    - preset: 02-migrate-from-confluent-cloud
      capacity_footprint:
        cpu_requests: 1500m
        memory_requests: 6Gi
        cpu_limits: "6"
        memory_limits: 12Gi
        basis: 3 workers x (500m CPU / 2Gi memory requests, 2 CPU / 4Gi limits)
      exclusions:
        - the dollar value of this capacity is the target cluster's node economics
        - the engine's internal storage topics (configs, offsets, status) live on the TARGET Kafka cluster and grow there
        - mirrored topic data lands on the target Kafka cluster and grows with source traffic across the mirroring window
    - preset: 03-active-passive-dr
      capacity_footprint:
        cpu_requests: "1"
        memory_requests: 4Gi
        cpu_limits: "4"
        memory_limits: 8Gi
        basis: 2 workers x (500m CPU / 2Gi memory requests, 2 CPU / 4Gi limits)
      exclusions:
        - the dollar value of this capacity is the target cluster's node economics
        - the engine's internal storage topics (configs, offsets, status) live on the TARGET Kafka cluster and grow there
        - mirrored topic data lands on the target Kafka cluster and grows with source traffic across the mirroring window
