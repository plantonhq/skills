# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azureprivatednsresolver
spec:
  presets:
    - preset: 01-hybrid-resolver
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Azure DNS
          sku_meter: inbound resolver endpoints
          pricing_unit: endpoint-months
          pricing_quantity: "1"
          quantity_basis: one flat monthly fee per declared inbound endpoint, prorated to hours if deleted earlier -- the rate is uniform across endpoints and identical in every Azure DNS billing geography, so endpoint count is the whole quantity
          list_unit_price: "180"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Azure DNS' and armRegionName eq 'Zone 1' and meterName eq 'Private Resolver Inbound Endpoint' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "180.0000"
        - service_name: Azure DNS
          sku_meter: outbound resolver endpoints
          pricing_unit: endpoint-months
          pricing_quantity: "1"
          quantity_basis: one flat monthly fee per declared outbound endpoint, the same list rate as the inbound meter -- an inbound-only resolver simply has zero of these
          list_unit_price: "180"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Azure DNS' and armRegionName eq 'Zone 1' and meterName eq 'Private Resolver Outbound Endpoint' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "180.0000"
      total_list_cost: "360.0000"
      exclusions:
        - DNS forwarding rulesets bill their own flat monthly fee on the AzurePrivateDnsResolverForwardingRuleset component that binds the outbound endpoints
        - the resolver adds no per-query meter -- queries answered from private zones bill those zones' per-million meter zone-side, and queries forwarded on-premises bill nothing in Azure
        - the anchor virtual network and the dedicated endpoint subnets are first-class components billing on their own lines
    - preset: 02-inbound-only
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Azure DNS
          sku_meter: inbound resolver endpoints
          pricing_unit: endpoint-months
          pricing_quantity: "1"
          quantity_basis: one flat monthly fee per declared inbound endpoint, prorated to hours if deleted earlier -- the rate is uniform across endpoints and identical in every Azure DNS billing geography, so endpoint count is the whole quantity
          list_unit_price: "180"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Azure DNS' and armRegionName eq 'Zone 1' and meterName eq 'Private Resolver Inbound Endpoint' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "180.0000"
      total_list_cost: "180.0000"
      exclusions:
        - DNS forwarding rulesets bill their own flat monthly fee on the AzurePrivateDnsResolverForwardingRuleset component that binds the outbound endpoints
        - the resolver adds no per-query meter -- queries answered from private zones bill those zones' per-million meter zone-side, and queries forwarded on-premises bill nothing in Azure
        - the anchor virtual network and the dedicated endpoint subnets are first-class components billing on their own lines
