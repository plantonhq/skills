# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azuremachinelearningonlinedeployment
spec:
  presets:
    - preset: 01-registered-model-deployment
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual Machines
          sku_meter: deployment instance VM hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: instance_count instances (the service default 1 when unspecified -- ARM carries the count as the deployment's SKU capacity) x 730 hours at the instance_type's Linux pay-as-you-go rate; managed deployments do not scale to zero
          list_unit_price: "0.293"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual Machines' and armRegionName eq 'eastus' and armSkuName eq 'Standard_DS3_v2' and meterName eq 'DS3 v2' and productName eq 'Virtual Machines DSv2 Series' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-16"
          list_cost: "213.8900"
      total_list_cost: "213.8900"
      exclusions:
        - network egress follows client placement
      notes: The endpoint address this deployment serves is free routing -- managed online endpoints carry no meter of their own, so the deployment's instances are where the spend lives. A blue/green pair bills both deployments' fleets for as long as both exist.
    - preset: 02-hardened-monitored-deployment
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual Machines
          sku_meter: deployment instance VM hours
          pricing_unit: hours
          pricing_quantity: "1460"
          quantity_basis: instance_count instances (the service default 1 when unspecified -- ARM carries the count as the deployment's SKU capacity) x 730 hours at the instance_type's Linux pay-as-you-go rate; managed deployments do not scale to zero
          list_unit_price: "0.293"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual Machines' and armRegionName eq 'eastus' and armSkuName eq 'Standard_DS3_v2' and meterName eq 'DS3 v2' and productName eq 'Virtual Machines DSv2 Series' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-16"
          list_cost: "427.7800"
      total_list_cost: "427.7800"
      exclusions:
        - captured scoring inputs and outputs land in the workspace's blob storage -- spend follows scoring traffic and each collection's sampling_rate, billed on the storage account
        - Application Insights ingestion for scoring-traffic telemetry follows request volume, billed on the workspace's Application Insights component
        - secure egress rides the workspace's managed network, whose private endpoints carry Private Link charges outside this component
        - network egress follows client placement
      notes: The endpoint address this deployment serves is free routing -- managed online endpoints carry no meter of their own, so the deployment's instances are where the spend lives. A blue/green pair bills both deployments' fleets for as long as both exist.
