# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awstransitgatewayvpcattachment
spec:
  presets:
    - preset: 01-mesh-spoke
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Virtual Private Cloud
          sku_meter: Transit Gateway attachment hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one VPC attachment, billed every hour of the month regardless of subnet count; every catalog preset leaves region a placeholder, so this prices the us-east-1 rate
          list_unit_price: "0.05"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonVPC/20260724154225/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "36.5000"
      total_list_cost: "36.5000"
      exclusions:
        - data processing bills per GB on every byte the gateway sends to or receives from this attachment (TransitGateway-Bytes) -- the dominant real-world cost on busy spokes, entirely traffic-dependent
      notes: The hourly fee is flat per attachment -- adding or removing subnets (Availability Zones) changes reachability, not the rate.
    - preset: 02-segmented-spoke
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Virtual Private Cloud
          sku_meter: Transit Gateway attachment hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one VPC attachment, billed every hour of the month regardless of subnet count; every catalog preset leaves region a placeholder, so this prices the us-east-1 rate
          list_unit_price: "0.05"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonVPC/20260724154225/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "36.5000"
      total_list_cost: "36.5000"
      exclusions:
        - data processing bills per GB on every byte the gateway sends to or receives from this attachment (TransitGateway-Bytes) -- the dominant real-world cost on busy spokes, entirely traffic-dependent
      notes: The hourly fee is flat per attachment -- adding or removing subnets (Availability Zones) changes reachability, not the rate.
    - preset: 03-inspection-vpc
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon Virtual Private Cloud
          sku_meter: Transit Gateway attachment hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one VPC attachment, billed every hour of the month regardless of subnet count; every catalog preset leaves region a placeholder, so this prices the us-east-1 rate
          list_unit_price: "0.05"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonVPC/20260724154225/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "36.5000"
      total_list_cost: "36.5000"
      exclusions:
        - data processing bills per GB on every byte the gateway sends to or receives from this attachment (TransitGateway-Bytes) -- the dominant real-world cost on busy spokes, entirely traffic-dependent
      notes: The hourly fee is flat per attachment -- adding or removing subnets (Availability Zones) changes reachability, not the rate.
