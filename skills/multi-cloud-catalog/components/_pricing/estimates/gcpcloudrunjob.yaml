# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file joins the component's estimate model (../models/, the authored
# quantities with their derivations) with the provider's price book
# (../pricebook/, pinned unit prices with source URL and retrieval date):
# every line cost and preset total is computed exactly, lines are ordered
# largest cost first, and the catalog-data conformance gate re-verifies all
# arithmetic. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: gcpcloudrunjob
spec:
  presets:
    - preset: 01-batch-etl-basic
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - a job definition bills nothing at rest -- each triggered execution runs one task (2 vCPU / 2Gi, up to 3600 seconds, no retries) billing $0.000018 per vCPU-second and $0.000002 per GiB-second at Cloud Run's instance-based job rate, minimum 1 minute per started instance
        - Google's monthly free tier (first 240,000 vCPU-seconds and 450,000 GiB-seconds per billing account) absorbs light schedules before the meters bill at all
        - the ETL's reads and writes bill on the systems the task code touches, and network egress follows Google Cloud networking pricing
      notes: Zero committed at creation -- the job runs only when something triggers it, and each nightly run's cost is its task lifetime, not a standing fee.
    - preset: 02-parallel-vpc-cleanup
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - each execution stamps out 10 tasks (1 vCPU / 512Mi, up to 600 seconds each, up to 2 retries) billing per vCPU-second and GiB-second only while they run; parallelism 5 halves the wall-clock, not the bill
        - direct VPC egress moves task traffic at VPC data transfer rates -- no standing connector cost exists in this preset
        - Secret Manager access operations for the API token resolve on the secret's project
      notes: The estimate is zero because nothing runs until triggered -- fan-out (task_count) multiplies per-execution cost, never idle cost.
    - preset: 03-gpu-batch-inference
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - each execution runs 8 GPU tasks (4 vCPU / 16Gi / one NVIDIA L4 each, up to 7200 seconds) -- the L4 without zonal redundancy bills $0.0001867 per second per task on top of the vCPU-second and GiB-second meters, the dominant cost whenever an execution runs
        - model artifacts and inference inputs/outputs bill on the storage components the tasks read and write
      notes: Zero committed at creation -- unlike a GPU service kept warm, a GPU job holds no capacity between executions; a full 2-hour, 8-task run is a per-run cost decision, not a monthly commitment.
