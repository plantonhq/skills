# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file joins the component's estimate model (../models/, the authored
# quantities with their derivations) with the provider's price book
# (../pricebook/, pinned unit prices with source URL and retrieval date):
# every line cost and preset total is computed exactly, lines are ordered
# largest cost first, and the catalog-data conformance gate re-verifies all
# arithmetic. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: gcppubsubsubscription
spec:
  presets:
    - preset: 01-basic-pull
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - pull delivery bills as Message Delivery at $40/TiB once the billing account's first free 10 GiB each month is spent
        - backlog storage bills $0.27/GiB-month only for unacked messages older than 1 day -- a consumer that keeps up pays nothing for storage
      notes: An idle subscription bills exactly zero -- the meters start with the first delivered byte, so the zero is commitment-absence, not freeness.
    - preset: 02-push-with-oidc
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - push delivery bills as Message Delivery at $40/TiB after the monthly free 10 GiB -- OIDC token minting adds no Pub/Sub rate
        - the consumer Cloud Run service's request handling bills on its own component; delivery to a consumer in another region adds inter-region data-transfer fees to this project
      notes: Zero committed at creation -- authenticated webhook delivery costs the same as anonymous; the spend follows the event volume pushed.
    - preset: 03-bigquery-delivery
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - BigQuery delivery bills subscription throughput at $50/TiB with NO monthly free allotment -- every delivered byte meters from the first
        - BigQuery storage on the written rows (and any later queries) bills on the table's component; Pub/Sub adds no separate BigQuery ingestion charge
      notes: Zero committed at creation -- the managed export premium ($50/TiB vs $40, no free tier) starts with the first message written to the table.
    - preset: 04-dead-letter
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - pull delivery bills $40/TiB after the monthly free 10 GiB; exactly-once delivery and the retry backoff add no rate
        - retain_acked_messages with the 14-day window bills message storage at $0.27/GiB-month on everything delivered and kept -- Google's priciest retention mechanism, chosen here deliberately for payment replay
        - messages dead-lettered after 10 failed attempts land on the DLQ topic and bill onward on that topic's and its subscriptions' components
      notes: Zero committed at creation -- at steady state the acked-retention storage is the preset's real cost line, roughly two weeks of delivered volume priced per GiB-month, and it scales with traffic, not time.
