# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awsfsxopenzfsfilesystem
spec:
  presets:
    - preset: 01-single-az-development
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon FSx
          sku_meter: provisioned throughput capacity
          pricing_unit: MiBps-months
          pricing_quantity: "64"
          quantity_basis: throughput_capacity MB/s of provisioned throughput, billed per MBps-month at the deployment type's rate (SINGLE_AZ_2, the spec default, assumed when unset); region is assumed us-east-1 when the manifest leaves it a placeholder
          list_unit_price: "0.26"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonFSx/20260805125901/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "16.6400"
        - service_name: Amazon FSx
          sku_meter: provisioned SSD storage
          pricing_unit: GB-months
          pricing_quantity: "64"
          quantity_basis: storage_capacity_gib GiB of provisioned SSD, billed per GB-month at the deployment type's rate (SINGLE_AZ_2, the spec default, assumed when unset); every catalog preset leaves region a placeholder, so this prices the us-east-1 rate
          list_unit_price: "0.09"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonFSx/20260805125901/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "5.7600"
      total_list_cost: "22.4000"
      exclusions:
        - data transfer to clients in other Availability Zones or out of AWS bills per GB outside FSx's meters
        - the final backup taken on deletion outlives the file system and bills per GB-month until deleted
      notes: SSD IOPS in AUTOMATIC mode (3 per GiB of storage) are included at no extra charge, and provisioned capacity bills full or empty -- compression stretches usable space, never the bill.
    - preset: 02-single-az-production
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon FSx
          sku_meter: provisioned throughput capacity
          pricing_unit: MiBps-months
          pricing_quantity: "640"
          quantity_basis: throughput_capacity MB/s of provisioned throughput, billed per MBps-month at the deployment type's rate (SINGLE_AZ_2, the spec default, assumed when unset); region is assumed us-east-1 when the manifest leaves it a placeholder
          list_unit_price: "0.26"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonFSx/20260805125901/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "166.4000"
        - service_name: Amazon FSx
          sku_meter: provisioned SSD storage
          pricing_unit: GB-months
          pricing_quantity: "512"
          quantity_basis: storage_capacity_gib GiB of provisioned SSD, billed per GB-month at the deployment type's rate (SINGLE_AZ_2, the spec default, assumed when unset); every catalog preset leaves region a placeholder, so this prices the us-east-1 rate
          list_unit_price: "0.09"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonFSx/20260805125901/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "46.0800"
      total_list_cost: "212.4800"
      exclusions:
        - data transfer to clients in other Availability Zones or out of AWS bills per GB outside FSx's meters
        - automatic backup storage bills per GB-month and grows with data churn across the retention window
        - the final backup taken on deletion outlives the file system and bills per GB-month until deleted
      notes: SSD IOPS in AUTOMATIC mode (3 per GiB of storage) are included at no extra charge, and provisioned capacity bills full or empty -- compression stretches usable space, never the bill.
