# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azureeventhubnamespace
spec:
  presets:
    - preset: 01-standard-streaming
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Event Hubs
          sku_meter: throughput units
          pricing_unit: hours
          pricing_quantity: "1460"
          quantity_basis: capacity TUs (Azure's default 1 when unset) x 730 hours at the Standard Throughput Unit rate -- this arm covers sku STANDARD and an unspecified sku, which deploys STANDARD
          list_unit_price: "0.03"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Event Hubs' and armRegionName eq 'eastus' and meterName eq 'Standard Throughput Unit' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-16"
          list_cost: "43.8000"
      total_list_cost: "43.8000"
      exclusions:
        - ingress events bill per million on the Standard Ingress Events meter and follow producer volume; capture, when a hub enables it, bills the hourly Standard Capture meter per TU
        - TUs auto-inflate grows above capacity bill at the same TU rate, up to maximum_throughput_units -- the estimate prices the provisioned floor
        - network egress follows consumer placement
      notes: A namespace placed on a dedicated cluster (dedicated_cluster_id) bills through the cluster's capacity units on its own AzureEventHubCluster component -- these rules refuse such a manifest rather than pricing tier meters the placement supersedes; the committed presets all deploy self-serve tiers.
    - preset: 02-locked-down-keyless
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Event Hubs
          sku_meter: throughput units
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: capacity TUs (Azure's default 1 when unset) x 730 hours at the Standard Throughput Unit rate -- this arm covers sku STANDARD and an unspecified sku, which deploys STANDARD
          list_unit_price: "0.03"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Event Hubs' and armRegionName eq 'eastus' and meterName eq 'Standard Throughput Unit' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-16"
          list_cost: "21.9000"
      total_list_cost: "21.9000"
      exclusions:
        - ingress events bill per million on the Standard Ingress Events meter and follow producer volume; capture, when a hub enables it, bills the hourly Standard Capture meter per TU
        - network egress follows consumer placement
      notes: A namespace placed on a dedicated cluster (dedicated_cluster_id) bills through the cluster's capacity units on its own AzureEventHubCluster component -- these rules refuse such a manifest rather than pricing tier meters the placement supersedes; the committed presets all deploy self-serve tiers.
    - preset: 03-premium-isolated
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Event Hubs
          sku_meter: processing units
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: capacity PUs (Azure sells 1, 2, 4, 8, or 16; default 1 when unset) x 730 hours at the Premium Processing Unit rate
          list_unit_price: "1.027"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Event Hubs' and armRegionName eq 'eastus' and meterName eq 'Premium Processing Unit' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-16"
          list_cost: "749.7100"
      total_list_cost: "749.7100"
      exclusions:
        - event data retained beyond the included window bills per GB-month on the Premium Extended Retention meter
        - network egress follows consumer placement
      notes: The price list publishes no Premium ingress or capture meters -- both are included in the PU rate. A namespace placed on a dedicated cluster (dedicated_cluster_id) bills through the cluster's capacity units on its own AzureEventHubCluster component -- these rules refuse such a manifest rather than pricing tier meters the placement supersedes; the committed presets all deploy self-serve tiers.
