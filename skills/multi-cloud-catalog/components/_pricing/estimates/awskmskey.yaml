# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awskmskey
spec:
  presets:
    - preset: 01-symmetric-encryption
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: AWS Key Management Service
          sku_meter: customer managed keys
          pricing_unit: key-months
          pricing_quantity: "1"
          quantity_basis: one customer-managed key holding one billed key-material version from creation, at the monthly per-key rate (AWS prorates it hourly); priced at the us-east-1 rate -- assumed when the manifest leaves region as a placeholder
          list_unit_price: "1"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/awskms/20250828153913/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "1.0000"
      total_list_cost: "1.0000"
      exclusions:
        - API requests bill per 10,000 beyond the 20,000-requests/month free tier (calculated across all regions) -- volume follows the workloads using the key; asymmetric and HMAC keys' requests bill at higher rates and are excluded from the free tier entirely, with key_spec selecting the class
        - each of the first two rotations adds another billed key-material version -- a full additional key-month charge arriving on the rotation_period_in_days cadence; AWS caps the surcharge at the second rotation
      notes: A long-rotated key settles at three key-months of charge per month -- the original version plus the two capped rotation versions. Aliases add no metered charge of their own -- the offer bills keys and API requests only.
    - preset: 02-cross-account-grants
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: AWS Key Management Service
          sku_meter: customer managed keys
          pricing_unit: key-months
          pricing_quantity: "1"
          quantity_basis: one customer-managed key holding one billed key-material version from creation, at the monthly per-key rate (AWS prorates it hourly); priced at the us-east-1 rate -- assumed when the manifest leaves region as a placeholder
          list_unit_price: "1"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/awskms/20250828153913/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "1.0000"
      total_list_cost: "1.0000"
      exclusions:
        - API requests bill per 10,000 beyond the 20,000-requests/month free tier (calculated across all regions) -- volume follows the workloads using the key; asymmetric and HMAC keys' requests bill at higher rates and are excluded from the free tier entirely, with key_spec selecting the class
        - each of the first two rotations adds another billed key-material version -- a full additional key-month charge arriving on the rotation_period_in_days cadence; AWS caps the surcharge at the second rotation
      notes: A long-rotated key settles at three key-months of charge per month -- the original version plus the two capped rotation versions. Aliases add no metered charge of their own -- the offer bills keys and API requests only. Grants add no metered charge of their own -- the offer bills keys and API requests only.
    - preset: 03-external-key-store
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: AWS Key Management Service
          sku_meter: customer managed keys
          pricing_unit: key-months
          pricing_quantity: "1"
          quantity_basis: one customer-managed key holding one billed key-material version from creation, at the monthly per-key rate (AWS prorates it hourly); priced at the us-east-1 rate -- assumed when the manifest leaves region as a placeholder
          list_unit_price: "1"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/awskms/20250828153913/us-east-1/index.json
          retrieved_on: "2026-08-16"
          list_cost: "1.0000"
      total_list_cost: "1.0000"
      exclusions:
        - API requests bill per 10,000 beyond the 20,000-requests/month free tier (calculated across all regions) -- volume follows the workloads using the key; asymmetric and HMAC keys' requests bill at higher rates and are excluded from the free tier entirely, with key_spec selecting the class
      notes: Aliases add no metered charge of their own -- the offer bills keys and API requests only.
