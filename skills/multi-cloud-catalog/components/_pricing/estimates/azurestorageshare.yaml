# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azurestorageshare
spec:
  presets:
    - preset: 01-team-file-share
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - used capacity bills 0.06 USD/GB-month (transaction-optimized Standard LRS, eastus -- Azure's default tier on standard accounts), and transactions bill 0.015 per 10K writes and lists, 0.0015 per 10K reads and protocol operations -- all following what workloads store and do on the account's meters
        - geo-redundant replication, data transfer, and private-endpoint hours bill on the account and networking components, not the share; snapshot-management backup fees are the AzureBackupProtectedFileshare component's
      notes: "Zero committed is real but rests on a stated assumption: with the tier unset, Azure defaults the share by ACCOUNT kind (TransactionOptimized on standard accounts, Premium on FileStorage) -- this derivation reads the share as standard, and a FileStorage parent would make the provisioned billing apply instead; the account kind is invisible in this manifest."
    - preset: 02-nfs-premium-share
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Storage
          sku_meter: provisioned share capacity
          pricing_unit: GB-months
          pricing_quantity: "100"
          quantity_basis: the premium provisioned model bills every provisioned GB per month from the moment the share exists, used or not -- quota_gb GB-months at the Premium LRS rate; LRS is a stated assumption (the referenced account's redundancy is invisible in this manifest; ZRS bills 0.20 USD/GB-month in eastus)
          list_unit_price: "0.16"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Storage' and armRegionName eq 'eastus' and meterName eq 'Premium LRS Provisioned' and productName eq 'Premium Files' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "16.0000"
      total_list_cost: "16.0000"
      exclusions:
        - share snapshots bill their differential used GB at 0.136 USD/GB-month (Premium LRS, eastus), following snapshot cadence and churn; an account on the provisioned v2 billing model bills separately provisioned storage/IOPS/throughput meters on its own rate card instead of the pinned v1 rate
        - geo-redundant replication, data transfer, and private-endpoint hours bill on the account and networking components, not the share; snapshot-management backup fees are the AzureBackupProtectedFileshare component's
      notes: "The committed figure is the provisioned floor: growing quota_gb grows the bill in place, and premium FileStorage floors at 100 GB. The rate is keyed to the ACCOUNT's redundancy, which this manifest sees only as a reference -- Premium LRS 0.16 USD/GB-month is assumed and stated; a ZRS account bills 0.20."
    - preset: 03-policy-anchored-access
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - used capacity bills 0.0228 USD/GB-month (Cool LRS, eastus) plus metadata at 0.0297, and every touch bills transactions (writes 0.13, reads 0.013, lists 0.065 per 10K) plus 0.01 USD/GB cool data retrieval -- all following what workloads store and do on the account's meters
        - geo-redundant replication, data transfer, and private-endpoint hours bill on the account and networking components, not the share; snapshot-management backup fees are the AzureBackupProtectedFileshare component's
