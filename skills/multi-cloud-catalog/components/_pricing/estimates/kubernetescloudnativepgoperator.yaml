# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its capacity derivation
# (../capacity/, the machine-executable value-to-capacity rules): each
# preset states the CPU/memory requests and limits and the persistent
# volume storage it reserves from its target cluster, with a basis sentence
# tracing every total back to the manifest's values. Cluster-capacity
# components carry no priced lines -- the dollar value of the reservation
# is the target cluster's node and storage-class economics, which this
# catalog cannot know. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: kubernetescloudnativepgoperator
spec:
  presets:
    - preset: 01-standard
      capacity_footprint:
        cpu_requests: 100m
        memory_requests: 256Mi
        cpu_limits: "1"
        memory_limits: 512Mi
        basis: 1 operator replica x (100m CPU / 256Mi memory requests, 1 CPU / 512Mi limits)
      exclusions:
        - the dollar value of this capacity is the target cluster's node economics
        - the PostgreSQL clusters this operator reconciles reserve their own capacity -- each rides its KubernetesPostgres resource
      notes: extra operator replicas are leader-elected warm standbys shortening operator failover -- they add standby capacity, not reconciliation throughput
    - preset: 02-with-backup-plugin
      capacity_footprint:
        cpu_requests: 150m
        memory_requests: 320Mi
        cpu_limits: 1250m
        memory_limits: 768Mi
        basis: 1 operator replica x (100m CPU / 256Mi memory requests, 1 CPU / 512Mi limits); 1 backup plugin x (50m CPU / 64Mi memory requests, 250m CPU / 256Mi limits)
      exclusions:
        - the dollar value of this capacity is the target cluster's node economics
        - the PostgreSQL clusters this operator reconciles reserve their own capacity -- each rides its KubernetesPostgres resource
      notes: extra operator replicas are leader-elected warm standbys shortening operator failover -- they add standby capacity, not reconciliation throughput
