# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: digitaloceankubernetescluster
spec:
  presets:
    - preset: 01-production-ha
      region_assumption: nyc3
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Kubernetes
          sku_meter: worker nodes
          pricing_unit: months
          pricing_quantity: "2"
          quantity_basis: the autoscaling FLOOR -- min_nodes worker droplets run and bill even at zero load; the ceiling is named in the exclusions, and node_count is only the initial count
          list_unit_price: "48.00"
          price_source: https://www.digitalocean.com/pricing/droplets
          retrieved_on: "2026-08-22"
          list_cost: "96.0000"
        - service_name: Kubernetes
          sku_meter: HA control plane
          pricing_unit: months
          pricing_quantity: "1"
          quantity_basis: the highly available control plane bills a flat 40.00 USD per month; the single-replica control plane (highly_available false) is free
          list_unit_price: "40.00"
          price_source: https://www.digitalocean.com/pricing/kubernetes
          retrieved_on: "2026-08-22"
          list_cost: "40.0000"
      total_list_cost: "136.0000"
      exclusions:
        - the cluster autoscaler grows the pool above the min_nodes floor up to max_nodes with workload demand -- each extra node bills the same monthly rate while it exists
        - outbound data transfer past the pooled free allowance (starting at 2,000 GiB per Basic node per month) bills 0.01 USD/GiB; inbound and node-to-node transfer are free
        - block storage volumes and load balancers created by the cluster's workloads (PersistentVolumeClaims, Services of type LoadBalancer) bill as their own DigitalOcean resources
        - additional node pools bill on their own DigitalOceanKubernetesNodePool components
      notes: Worker nodes bill at DigitalOcean's droplet rates -- the DOKS pricing page charges only the underlying droplets and prices per single node; the control plane, version updates, and the autoscaler are free. Priced at DigitalOcean's global rates -- the DOKS and droplet pricing pages carry no region dimension.
    - preset: 02-development
      region_assumption: nyc3
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Kubernetes
          sku_meter: worker nodes
          pricing_unit: months
          pricing_quantity: "2"
          quantity_basis: a fixed pool of node_count worker droplets, each billing the size slug's flat monthly droplet rate from creation
          list_unit_price: "24.00"
          price_source: https://www.digitalocean.com/pricing/droplets
          retrieved_on: "2026-08-22"
          list_cost: "48.0000"
      total_list_cost: "48.0000"
      exclusions:
        - outbound data transfer past the pooled free allowance (starting at 2,000 GiB per Basic node per month) bills 0.01 USD/GiB; inbound and node-to-node transfer are free
        - block storage volumes and load balancers created by the cluster's workloads (PersistentVolumeClaims, Services of type LoadBalancer) bill as their own DigitalOcean resources
        - additional node pools bill on their own DigitalOceanKubernetesNodePool components
      notes: Worker nodes bill at DigitalOcean's droplet rates -- the DOKS pricing page charges only the underlying droplets and prices per single node; the control plane, version updates, and the autoscaler are free. Priced at DigitalOcean's global rates -- the DOKS and droplet pricing pages carry no region dimension.
