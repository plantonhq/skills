# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azurevirtualmachinescaleset
spec:
  presets:
    - preset: 01-stateless-web-flexible
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual Machines
          sku_meter: Fleet compute
          pricing_unit: hours
          pricing_quantity: "2190"
          quantity_basis: instances x 730 hours at the SKU's base pay-as-you-go rate (the Linux rate; Windows fleets reach this line only with Azure Hybrid Benefit) -- a floor when autoscaling manages capacity, since the real count follows the autoscaler
          list_unit_price: "0.096"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual Machines' and armRegionName eq 'eastus' and armSkuName eq 'Standard_D2s_v3' and meterName eq 'D2s v3' and productName eq 'Virtual Machines DSv3 Series' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-16"
          list_cost: "210.2400"
      total_list_cost: "210.2400"
      exclusions:
        - fleet-wide data disks (data_disks) bill one managed disk per instance at each definition's tier
        - instance-level public IPs configured inline bill per address; referenced subnets, load balancer pools, and application gateway pools bill on their own components
        - network egress follows traffic and client placement
      notes: Ephemeral OS disks live on instance-local storage and add no managed-disk charge, on any fleet size.
    - preset: 03-windows-uniform-rolling
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual Machines
          sku_meter: Fleet compute
          pricing_unit: hours
          pricing_quantity: "2190"
          quantity_basis: instances x 730 hours at the SKU's base pay-as-you-go rate (the Linux rate; Windows fleets reach this line only with Azure Hybrid Benefit) -- a floor when autoscaling manages capacity, since the real count follows the autoscaler
          list_unit_price: "0.096"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual Machines' and armRegionName eq 'eastus' and armSkuName eq 'Standard_D2s_v3' and meterName eq 'D2s v3' and productName eq 'Virtual Machines DSv3 Series' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-16"
          list_cost: "210.2400"
      total_list_cost: "210.2400"
      exclusions:
        - each instance's managed OS disk bills at the tier matching the configured storage SKU and size (image-inherited when unset), a per-instance managed-disk meter not yet priced here
        - fleet-wide data disks (data_disks) bill one managed disk per instance at each definition's tier
        - instance-level public IPs configured inline bill per address; referenced subnets, load balancer pools, and application gateway pools bill on their own components
        - network egress follows traffic and client placement
      notes: Azure Hybrid Benefit prices every instance at the base compute rate; the Windows Server license obligation is carried by the customer's own licensing, not this bill.
