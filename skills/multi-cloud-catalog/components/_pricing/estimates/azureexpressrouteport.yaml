# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azureexpressrouteport
spec:
  presets:
    - preset: 01-standard-port
      region_assumption: zone-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: ExpressRoute
          sku_meter: Direct port pair
          pricing_unit: port-months
          pricing_quantity: "1"
          quantity_basis: an unspecified billing type deploys METERED_DATA (ARM's default) -- one port pair x the 10 Gbps metered monthly fee, Zone 1 assumed as above
          list_unit_price: "6000"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'ExpressRoute' and armRegionName eq 'Zone 1' and meterName eq 'Port 10 Gbps Port' and productName eq 'ExpressRoute Direct' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "6000.0000"
      total_list_cost: "6000.0000"
      exclusions:
        - circuits carved from the port bill on their own components -- Premium circuit add-ons carry a monthly fee, and on metered ports the circuits' outbound traffic bills per GB
        - the physical cross-connect and colocation charges are contracted with the facility operator, outside Azure entirely
        - MACsec key reads bill as Key Vault operations on the referenced vault's component
      notes: The port fee is priced by the peering location's zone (Zone 1 through Zone 4) -- facilities outside Zone 1 bill higher list rates on the SAME meters, and no committed mapping from facility name to zone exists in the manifest, which is why the zone rides as a stated assumption rather than a lookup. MACsec, link admin state, issued authorizations, and the managed identity add no meters -- the port pair fee is the whole committed bill.
    - preset: 02-macsec-port
      region_assumption: zone-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: ExpressRoute
          sku_meter: Direct port pair
          pricing_unit: port-months
          pricing_quantity: "1"
          quantity_basis: an unspecified billing type deploys METERED_DATA (ARM's default) -- one port pair x the 10 Gbps metered monthly fee, Zone 1 assumed as above
          list_unit_price: "6000"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'ExpressRoute' and armRegionName eq 'Zone 1' and meterName eq 'Port 10 Gbps Port' and productName eq 'ExpressRoute Direct' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "6000.0000"
      total_list_cost: "6000.0000"
      exclusions:
        - circuits carved from the port bill on their own components -- Premium circuit add-ons carry a monthly fee, and on metered ports the circuits' outbound traffic bills per GB
        - the physical cross-connect and colocation charges are contracted with the facility operator, outside Azure entirely
        - MACsec key reads bill as Key Vault operations on the referenced vault's component
      notes: The port fee is priced by the peering location's zone (Zone 1 through Zone 4) -- facilities outside Zone 1 bill higher list rates on the SAME meters, and no committed mapping from facility name to zone exists in the manifest, which is why the zone rides as a stated assumption rather than a lookup. MACsec, link admin state, issued authorizations, and the managed identity add no meters -- the port pair fee is the whole committed bill.
    - preset: 03-shared-capacity-port
      region_assumption: zone-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: ExpressRoute
          sku_meter: Direct port pair
          pricing_unit: port-months
          pricing_quantity: "1"
          quantity_basis: one port pair x the 100 Gbps unlimited-data monthly fee -- the flat rate that removes per-GB egress metering from every circuit on the port; Zone 1 assumed as above
          list_unit_price: "250000"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'ExpressRoute' and armRegionName eq 'Zone 1' and meterName eq 'Port Unlimited 100 Gbps Port' and productName eq 'ExpressRoute Direct' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "250000.0000"
      total_list_cost: "250000.0000"
      exclusions:
        - circuits carved from the port bill on their own components -- Premium circuit add-ons carry a monthly fee, and on metered ports the circuits' outbound traffic bills per GB
        - the physical cross-connect and colocation charges are contracted with the facility operator, outside Azure entirely
        - MACsec key reads bill as Key Vault operations on the referenced vault's component
      notes: The port fee is priced by the peering location's zone (Zone 1 through Zone 4) -- facilities outside Zone 1 bill higher list rates on the SAME meters, and no committed mapping from facility name to zone exists in the manifest, which is why the zone rides as a stated assumption rather than a lookup. MACsec, link admin state, issued authorizations, and the managed identity add no meters -- the port pair fee is the whole committed bill. Billing type is the one major dial that is NOT fixed at creation -- Azure allows switching a port between metered and unlimited in place, so the committed fee can be re-tuned as utilization data accumulates.
