# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azuremonitorscheduledqueryalert
spec:
  presets:
    - preset: 01-error-spike
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Azure Monitor
          sku_meter: log alert time series
          pricing_unit: time-series-months
          pricing_quantity: "1"
          quantity_basis: each criterion is its own scheduled query evaluating one time series at the 5-minute frequency rate per month -- the rule's fee includes each criterion's first series
          list_unit_price: "1.5"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Azure Monitor' and armRegionName eq 'eastus' and meterName eq 'Alerts System Log Monitored at 5 Minute Frequency' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "1.5000"
      total_list_cost: "1.5000"
      exclusions:
        - dimension splits (at-scale log monitoring) bill each evaluated dimension combination beyond the included first series at the same frequency rate -- a count that follows the live data's values, which is why a "*" split's bill cannot be committed here
        - notifications the fired alert sends (email, SMS, voice, webhooks beyond the free monthly allowances) bill on the referenced AzureMonitorActionGroup components
        - the queried workspace's ingestion and retention bill on the AzureLogAnalyticsWorkspace component
      notes: The fee accrues from rule creation until deletion whether the alert ever fires or not; enabled=false suspends both evaluation and billing, which is why pausing beats deleting during maintenance windows.
    - preset: 02-latency-threshold
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Azure Monitor
          sku_meter: log alert time series
          pricing_unit: time-series-months
          pricing_quantity: "1"
          quantity_basis: each criterion is its own scheduled query evaluating one time series at the 5-minute frequency rate per month -- the rule's fee includes each criterion's first series
          list_unit_price: "1.5"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Azure Monitor' and armRegionName eq 'eastus' and meterName eq 'Alerts System Log Monitored at 5 Minute Frequency' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "1.5000"
      total_list_cost: "1.5000"
      exclusions:
        - dimension splits (at-scale log monitoring) bill each evaluated dimension combination beyond the included first series at the same frequency rate -- a count that follows the live data's values, which is why a "*" split's bill cannot be committed here
        - notifications the fired alert sends (email, SMS, voice, webhooks beyond the free monthly allowances) bill on the referenced AzureMonitorActionGroup components
        - the queried workspace's ingestion and retention bill on the AzureLogAnalyticsWorkspace component
      notes: The fee accrues from rule creation until deletion whether the alert ever fires or not; enabled=false suspends both evaluation and billing, which is why pausing beats deleting during maintenance windows.
    - preset: 03-missing-heartbeat
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Azure Monitor
          sku_meter: log alert time series
          pricing_unit: time-series-months
          pricing_quantity: "1"
          quantity_basis: each criterion is its own scheduled query evaluating one time series at the 5-minute frequency rate per month -- the rule's fee includes each criterion's first series
          list_unit_price: "1.5"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Azure Monitor' and armRegionName eq 'eastus' and meterName eq 'Alerts System Log Monitored at 5 Minute Frequency' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "1.5000"
      total_list_cost: "1.5000"
      exclusions:
        - dimension splits (at-scale log monitoring) bill each evaluated dimension combination beyond the included first series at the same frequency rate -- a count that follows the live data's values, which is why a "*" split's bill cannot be committed here
        - notifications the fired alert sends (email, SMS, voice, webhooks beyond the free monthly allowances) bill on the referenced AzureMonitorActionGroup components
        - the queried workspace's ingestion and retention bill on the AzureLogAnalyticsWorkspace component
      notes: The fee accrues from rule creation until deletion whether the alert ever fires or not; enabled=false suspends both evaluation and billing, which is why pausing beats deleting during maintenance windows.
