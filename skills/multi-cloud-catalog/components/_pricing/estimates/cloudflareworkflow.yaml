# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file joins the component's estimate model (../models/, the authored
# quantities with their derivations) with the provider's price book
# (../pricebook/, pinned unit prices with source URL and retrieval date):
# every line cost and preset total is computed exactly, lines are ordered
# largest cost first, and the catalog-data conformance gate re-verifies all
# arithmetic. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: cloudflareworkflow
spec:
  presets:
    - preset: 01-scheduled-workflow
      region_assumption: global
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - the "Workers requests and CPU time" meter (instance runs -- requests plus CPU milliseconds) bills per instance the nightly cron trigger creates, metered on the account's Workers plan; free-tier limits apply, and production-scale usage rides Workers Paid ($5/mo) limits per the component's cost profile
        - the "Workers CPU time and storage" meter (steps executed per instance, bounded here at 512) depends on what each step actually does, not on this manifest
      notes: Zero committed cost -- registering the workflow starts no meter; spend begins only when instances run, and instance volume and per-instance compute are traffic- and code-driven.
