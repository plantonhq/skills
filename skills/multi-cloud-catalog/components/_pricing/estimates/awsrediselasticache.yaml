# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file joins the component's estimate model (../models/, the authored
# quantities with their derivations) with the provider's price book
# (../pricebook/, pinned unit prices with source URL and retrieval date):
# every line cost and preset total is computed exactly, lines are ordered
# largest cost first, and the catalog-data conformance gate re-verifies all
# arithmetic. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awsrediselasticache
spec:
  presets:
    - preset: 01-redis-single-node
      region_assumption: us-west-2
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon ElastiCache
          sku_meter: cache node hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: numCacheClusters 1 -- a single cache.t3.micro node, billed every hour of the month
          list_unit_price: "0.017"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonElastiCache/20260813190319/us-west-2/index.json
          retrieved_on: "2026-08-14"
          list_cost: "12.4100"
      total_list_cost: "12.4100"
      exclusions:
        - no snapshots are configured, so no snapshot storage accrues
        - data transfer between AZs does not apply to a single node
      notes: A development cache -- one burstable node, no replicas, no failover.
    - preset: 02-redis-ha-cluster
      region_assumption: us-west-2
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon ElastiCache
          sku_meter: cache node hours
          pricing_unit: hours
          pricing_quantity: "2190"
          quantity_basis: numCacheClusters 3 -- one primary plus two replicas on cache.r7g.large, 3 nodes x 730 hours
          list_unit_price: "0.219"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonElastiCache/20260813190319/us-west-2/index.json
          retrieved_on: "2026-08-14"
          list_cost: "479.6100"
      total_list_cost: "479.6100"
      exclusions:
        - snapshot storage beyond the free allowance across the 7-day retention grows with dataset size
        - cross-AZ replication transfer between the multi-AZ nodes is write-volume-dependent
      notes: Multi-AZ failover carries no fee of its own -- its cost is the two replica nodes.
    - preset: 03-redis-clustered-production
      region_assumption: us-west-2
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon ElastiCache
          sku_meter: cache node hours
          pricing_unit: hours
          pricing_quantity: "6570"
          quantity_basis: numNodeGroups 3 x (1 primary + replicasPerNodeGroup 2) = 9 cache.r7g.xlarge nodes x 730 hours
          list_unit_price: "0.437"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonElastiCache/20260813190319/us-west-2/index.json
          retrieved_on: "2026-08-14"
          list_cost: "2871.0900"
      total_list_cost: "2871.0900"
      exclusions:
        - snapshot storage beyond the free allowance across the 14-day retention grows with dataset size
        - slow-log delivery bills CloudWatch Logs ingestion with engine activity
        - cross-AZ replication transfer is write-volume-dependent
      notes: The largest committed spend in this component's presets -- nine memory-optimized nodes around the clock before a single command runs.
