# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azureexpressroutegateway
spec:
  presets:
    - preset: 01-standard-gateway
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual WAN
          sku_meter: ExpressRoute scale units
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: the gateway bills its configured scale units (about 2 Gbps of circuit-to-WAN throughput each; the spec requires an explicit 1-10 value, and scaling is a manual operation in Virtual WAN, so the configured value is what bills) x 730 hours at the flat first-five rate -- units 6-10 refuse above
          list_unit_price: "0.42"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual WAN' and armRegionName eq 'eastus' and meterName eq 'ExpressRoute Scale Unit' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "306.6000"
      total_list_cost: "306.6000"
      exclusions:
        - the hub's deployment hours and per-GB data processing bill on the AzureVirtualHub component
        - the circuit's port and peering charges, and outbound data on metered circuit SKUs, bill on the circuit's own components
        - data leaving Azure over the circuits bills at ExpressRoute data-transfer rates and follows traffic
    - preset: 02-circuit-connection
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Virtual WAN
          sku_meter: ExpressRoute scale units
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: the gateway bills its configured scale units (about 2 Gbps of circuit-to-WAN throughput each; the spec requires an explicit 1-10 value, and scaling is a manual operation in Virtual WAN, so the configured value is what bills) x 730 hours at the flat first-five rate -- units 6-10 refuse above
          list_unit_price: "0.42"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual WAN' and armRegionName eq 'eastus' and meterName eq 'ExpressRoute Scale Unit' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "306.6000"
        - service_name: Virtual WAN
          sku_meter: ExpressRoute connection units
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: each composed connection joins one circuit peering to the hub and bills one ExpressRoute connection unit per hour -- Microsoft counts circuit connections, uniformly per connection, so the repeated field's element count is the quantity; a connectionless gateway emits nothing here
          list_unit_price: "0.05"
          price_source: "https://prices.azure.com/api/retail/prices?api-version=2023-01-01-preview&currencyCode='USD'&$filter=serviceName eq 'Virtual WAN' and armRegionName eq 'eastus' and meterName eq 'ExpressRoute Connection Unit' and priceType eq 'Consumption'"
          retrieved_on: "2026-08-18"
          list_cost: "36.5000"
      total_list_cost: "343.1000"
      exclusions:
        - the hub's deployment hours and per-GB data processing bill on the AzureVirtualHub component
        - the circuit's port and peering charges, and outbound data on metered circuit SKUs, bill on the circuit's own components
        - data leaving Azure over the circuits bills at ExpressRoute data-transfer rates and follows traffic
      notes: The per-connection FastPath and internet-security toggles are free configuration -- the complete Virtual WAN price list carries no meter for either.
