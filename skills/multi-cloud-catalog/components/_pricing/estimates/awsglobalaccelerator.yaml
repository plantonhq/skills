# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awsglobalaccelerator
spec:
  presets:
    - preset: 01-basic-tcp-accelerator
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: AWS Global Accelerator
          sku_meter: accelerator hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one accelerator, billed the fixed hourly fee every hour of the month -- enabled or disabled, the fee runs until deletion; the rate is global (the offer prices it for location "Any")
          list_unit_price: "0.025"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AWSGlobalAccelerator/20230914175116/aws-other/index.json
          retrieved_on: "2026-08-16"
          list_cost: "18.2500"
        - service_name: Amazon Virtual Private Cloud
          sku_meter: public IPv4 address usage
          pricing_unit: IP-hours
          pricing_quantity: "1460"
          quantity_basis: two AWS-allocated static anycast IPv4 addresses, each billed as an in-use public IPv4 address every hour of the month; the standard rate is uniform across regions and priced here from the us-east-1 entry
          list_unit_price: "0.005"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonVPC/20260724154225/us-east-1/index.json
          retrieved_on: "2026-08-14"
          list_cost: "7.3000"
      total_list_cost: "25.5500"
      exclusions:
        - Data Transfer-Premium bills per GB in the dominant traffic direction, at a rate varying by source region and destination edge -- entirely traffic-dependent
        - standard data transfer out at the endpoint resources (ALB, NLB, EC2) bills on those components, in addition to DT-Premium
      notes: Listeners and endpoint groups carry no committed fee of their own -- the offer's only fixed-fee meter is the per-accelerator hourly charge; their counts only shape which DT-Premium rate rows traffic bills at.
    - preset: 02-multi-region-production
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: AWS Global Accelerator
          sku_meter: accelerator hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one accelerator, billed the fixed hourly fee every hour of the month -- enabled or disabled, the fee runs until deletion; the rate is global (the offer prices it for location "Any")
          list_unit_price: "0.025"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AWSGlobalAccelerator/20230914175116/aws-other/index.json
          retrieved_on: "2026-08-16"
          list_cost: "18.2500"
        - service_name: Amazon Virtual Private Cloud
          sku_meter: public IPv4 address usage
          pricing_unit: IP-hours
          pricing_quantity: "1460"
          quantity_basis: two AWS-allocated static anycast IPv4 addresses, each billed as an in-use public IPv4 address every hour of the month; the standard rate is uniform across regions and priced here from the us-east-1 entry
          list_unit_price: "0.005"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonVPC/20260724154225/us-east-1/index.json
          retrieved_on: "2026-08-14"
          list_cost: "7.3000"
      total_list_cost: "25.5500"
      exclusions:
        - Data Transfer-Premium bills per GB in the dominant traffic direction, at a rate varying by source region and destination edge -- entirely traffic-dependent
        - standard data transfer out at the endpoint resources (ALB, NLB, EC2) bills on those components, in addition to DT-Premium
        - flow-log storage and request charges bill to the destination S3 bucket, not the accelerator
      notes: Listeners and endpoint groups carry no committed fee of their own -- the offer's only fixed-fee meter is the per-accelerator hourly charge; their counts only shape which DT-Premium rate rows traffic bills at.
    - preset: 03-gaming-udp-accelerator
      region_assumption: us-east-1
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: AWS Global Accelerator
          sku_meter: accelerator hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one accelerator, billed the fixed hourly fee every hour of the month -- enabled or disabled, the fee runs until deletion; the rate is global (the offer prices it for location "Any")
          list_unit_price: "0.025"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AWSGlobalAccelerator/20230914175116/aws-other/index.json
          retrieved_on: "2026-08-16"
          list_cost: "18.2500"
        - service_name: Amazon Virtual Private Cloud
          sku_meter: public IPv4 address usage
          pricing_unit: IP-hours
          pricing_quantity: "1460"
          quantity_basis: two AWS-allocated static anycast IPv4 addresses, each billed as an in-use public IPv4 address every hour of the month; the standard rate is uniform across regions and priced here from the us-east-1 entry
          list_unit_price: "0.005"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonVPC/20260724154225/us-east-1/index.json
          retrieved_on: "2026-08-14"
          list_cost: "7.3000"
      total_list_cost: "25.5500"
      exclusions:
        - Data Transfer-Premium bills per GB in the dominant traffic direction, at a rate varying by source region and destination edge -- entirely traffic-dependent
        - standard data transfer out at the endpoint resources (ALB, NLB, EC2) bills on those components, in addition to DT-Premium
      notes: Listeners and endpoint groups carry no committed fee of their own -- the offer's only fixed-fee meter is the per-accelerator hourly charge; their counts only shape which DT-Premium rate rows traffic bills at.
