# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file joins the component's estimate model (../models/, the authored
# quantities with their derivations) with the provider's price book
# (../pricebook/, pinned unit prices with source URL and retrieval date):
# every line cost and preset total is computed exactly, lines are ordered
# largest cost first, and the catalog-data conformance gate re-verifies all
# arithmetic. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: gcppubsubtopic
spec:
  presets:
    - preset: 01-basic-topic
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - publish throughput bills as Message Delivery at $40/TiB once the billing account's first free 10 GiB each month is spent
        - no retention is configured, so no storage meter can start on the topic; delivery and any backlog bill on the subscription components
      notes: An idle topic bills exactly zero -- the meter starts with the first published byte, so the zero is commitment-absence, not freeness.
    - preset: 02-regional-encrypted
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - publish throughput bills $40/TiB after the monthly free 10 GiB, exactly as for an unencrypted topic -- CMEK adds no Pub/Sub rate
        - Cloud KMS encrypt/decrypt operations for the CMEK key bill on the GcpKmsKey component
        - the single-region storage policy reroutes messages published from other regions, billing inter-region data transfer to this topic's project per rerouted GiB; enforce_in_transit rejects such publishes instead, so the transfer meter only runs for compliant-but-remote producers
      notes: Zero committed at creation -- residency and key custody are posture choices, not rate changes; the audit stream's cost follows its volume.
    - preset: 03-message-retention
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - every published message is retained 7 days and bills message storage at $0.27/GiB-month for the window -- storage follows publish volume, and an idle topic retains nothing
        - publish throughput bills $40/TiB after the monthly free 10 GiB
      notes: The 7-day replay window commits nothing until messages arrive; at steady state the storage bill is roughly the week's publish volume priced at the monthly storage rate pro-rated.
    - preset: 04-cloud-storage-ingestion
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - as an import topic, ingested data bills publish throughput at the Cloud Storage import rate of $80/TiB, and the first-10-GiB monthly free allotment does NOT apply to import throughput
        - Cloud Storage read operations performed during ingestion bill on the source bucket's component
        - ingestion platform logs at INFO severity bill as Cloud Logging ingestion in proportion to pipeline activity
      notes: Zero committed at creation -- the meters start when matching objects land in the bucket, and the import premium ($80/TiB vs $40) is the price of the managed pipeline.
    - preset: 05-schema-validated
      region_assumption: us-central1
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - publish throughput for accepted messages bills $40/TiB after the monthly free 10 GiB; non-conforming messages are rejected at the publisher with INVALID_ARGUMENT and never reach subscribers
        - the referenced schema is a separate free-at-rest component (GcpPubSubSchema)
      notes: Zero committed at creation -- publish-time contract enforcement changes what gets in, not what it costs.
