# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file replays the component's presets through its cost derivation
# (../derivations/, the machine-executable value-to-quantity rules) against
# the provider's price book (../pricebook/, pinned unit prices with source
# URL and retrieval date): every line cost and preset total is computed
# exactly, lines are ordered largest cost first, and the catalog-data
# conformance gate re-verifies all arithmetic. A preset the derivation
# refuses to price (a rate delegated to a referenced resource) carries no
# entry here -- absence is the honest state, never a guessed number.
# Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: awssagemakernotebookinstance
spec:
  presets:
    - preset: 01-starter-notebook
      region_assumption: us-west-2
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon SageMaker
          sku_meter: notebook instance hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one notebook instance x 730 hours at the instance_type's notebook rate -- the always-on reading of a bill that actually pauses whenever the instance is stopped
          list_unit_price: "0.05"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonSageMaker/20260815025356/us-west-2/index.json
          retrieved_on: "2026-08-16"
          list_cost: "36.5000"
        - service_name: Amazon SageMaker
          sku_meter: notebook ML storage
          pricing_unit: GB-months
          pricing_quantity: "20"
          quantity_basis: the ML storage volume (volume_size_gb, the AWS default 5 GB when the manifest does not size it) billed per GB-month for as long as the instance exists, stopped or running
          list_unit_price: "0.14"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonSageMaker/20260815025356/us-west-2/index.json
          retrieved_on: "2026-08-16"
          list_cost: "2.8000"
      total_list_cost: "39.3000"
      exclusions:
        - hours while the instance is stopped bill no instance-hours -- the estimate assumes 24x7 InService because no manifest field expresses a run schedule
        - data transfer out of the region and to the internet follows whatever the notebook's code moves
        - CloudWatch Logs ingestion for Jupyter and lifecycle-script logs follows log volume
        - AWS resources the notebook's code creates through its execution role bill on their own
      notes: Estimated at the always-on reading of an always-on billing model -- the notebook runs the full month unless someone stops it; stopping is the savings lever, and the storage line keeps billing even then.
    - preset: 02-locked-down-gpu-notebook
      region_assumption: us-west-2
      currency: USD
      hours_per_month: 730
      line_items:
        - service_name: Amazon SageMaker
          sku_meter: notebook instance hours
          pricing_unit: hours
          pricing_quantity: "730"
          quantity_basis: one notebook instance x 730 hours at the instance_type's notebook rate -- the always-on reading of a bill that actually pauses whenever the instance is stopped
          list_unit_price: "0.736"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonSageMaker/20260815025356/us-west-2/index.json
          retrieved_on: "2026-08-16"
          list_cost: "537.2800"
        - service_name: Amazon SageMaker
          sku_meter: notebook ML storage
          pricing_unit: GB-months
          pricing_quantity: "100"
          quantity_basis: the ML storage volume (volume_size_gb, the AWS default 5 GB when the manifest does not size it) billed per GB-month for as long as the instance exists, stopped or running
          list_unit_price: "0.14"
          price_source: https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonSageMaker/20260815025356/us-west-2/index.json
          retrieved_on: "2026-08-16"
          list_cost: "14.0000"
      total_list_cost: "551.2800"
      exclusions:
        - hours while the instance is stopped bill no instance-hours -- the estimate assumes 24x7 InService because no manifest field expresses a run schedule
        - data transfer out of the region and to the internet follows whatever the notebook's code moves
        - AWS resources the notebook's code creates through its execution role bill on their own
      notes: Estimated at the always-on reading of an always-on billing model -- the notebook runs the full month unless someone stops it; stopping is the savings lever, and the storage line keeps billing even then. Cloning the code repositories adds no charge of its own.
