# GENERATED -- DO NOT EDIT. Regenerate with `make generate-cost-estimates`.
# This file joins the component's estimate model (../models/, the authored
# quantities with their derivations) with the provider's price book
# (../pricebook/, pinned unit prices with source URL and retrieval date):
# every line cost and preset total is computed exactly, lines are ordered
# largest cost first, and the catalog-data conformance gate re-verifies all
# arithmetic. Schema: finops/componentcostestimate/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostEstimate
metadata:
  name: azureeventgrideventsubscription
spec:
  presets:
    - preset: 01-queue-fan-out
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - every delivery attempt to the storage queue bills the source topic's Basic operations meter at 0.60 USD per million past its monthly 100,000-operation free grant, counted in 64 KB units, retries included
        - dead-lettered events bill as delivery operations, and each parked event is a blob whose storage and write transactions bill on the dead-letter storage account's own component
        - the queue messages the deliveries become bill storage-queue transactions on the destination storage account's own component
        - no billable filters are configured -- this subscription delivers everything the topic emits, so filtering adds nothing to the meter
      notes: Zero committed cost -- the subscription holds for free, and the production-grade dead-letter posture costs nothing until an event actually fails its retries.
    - preset: 02-filtered-webhook
      region_assumption: eastus
      currency: USD
      hours_per_month: 730
      total_list_cost: "0.00"
      exclusions:
        - every delivery attempt to the webhook bills the source topic's Basic operations meter at 0.60 USD per million past its monthly free grant -- with the tuned retry policy capping a failing endpoint at 10 attempts per event instead of Azure's default 30
        - the advanced filter (data.amount greater-than) bills one operation per evaluation, including for events it rejects; the subject-BEGINS-with filter is free by Microsoft's pricing FAQ
        - dead-lettered events bill as delivery operations, with blob charges on the dead-letter storage account's own component
        - the webhook handler's own compute is wherever it runs -- never on this meter
      notes: Zero committed cost -- filters, delivery schema, and retry tuning are free to declare; the advanced filter starts billing only when events start arriving to evaluate.
