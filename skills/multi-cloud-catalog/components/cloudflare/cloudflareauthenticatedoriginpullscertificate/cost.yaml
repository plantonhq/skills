# Cost profile: what drives this component's bill. Field paths are
# spec-relative proto field names, held to the served contract by the
# catalog-data conformance gate. Schema: finops/componentcostprofile/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostProfile
metadata:
  name: cloudflareauthenticatedoriginpullscertificate
spec:
  billing_model: free
  notes: Authenticated Origin Pulls client-certificate uploads are free on every plan, on both the zone and hostname surfaces. Certificate issuance (your PKI or openssl) is outside Cloudflare entirely.
