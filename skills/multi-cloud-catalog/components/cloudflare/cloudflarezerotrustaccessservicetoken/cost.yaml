# Cost profile: what drives this component's bill. Field paths are
# spec-relative proto field names, held to the served contract by the
# catalog-data conformance gate. Schema: finops/componentcostprofile/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostProfile
metadata:
  name: cloudflarezerotrustaccessservicetoken
spec:
  billing_model: free
  notes: Access service tokens are included in Zero Trust Free. Creating or rotating a token creates no charge of its own. Seat counts and paid Zero Trust add-ons bill on the organization, never through this component.
