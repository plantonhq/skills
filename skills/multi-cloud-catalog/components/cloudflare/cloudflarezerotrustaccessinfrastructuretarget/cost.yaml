# Cost profile: what drives this component's bill. Field paths are
# spec-relative proto field names, held to the served contract by the
# catalog-data conformance gate. Schema: finops/componentcostprofile/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostProfile
metadata:
  name: cloudflarezerotrustaccessinfrastructuretarget
spec:
  billing_model: free
  notes: >-
    Target registrations are free -- the inventory rows create no charge.
    Zero Trust seat billing is driven by the USERS who connect through
    infrastructure applications, never by the number of registered targets.
