# Cost profile: what drives this component's bill. Field paths are
# spec-relative proto field names, held to the served contract by the
# catalog-data conformance gate. Schema: finops/componentcostprofile/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostProfile
metadata:
  name: cloudflarezerotrustmcpserver
spec:
  billing_model: free
  notes: >-
    MCP server registrations create no charge of their own. Zero Trust seat
    billing is driven by the USERS who access portals, and any Secure Web
    Gateway filtering rides the account's existing Gateway posture.
