# Least-privilege permissions the IaC runner's Cloudflare credential needs
# to provision, update, and destroy this component with the official
# modules. Cloudflare authenticates with a bearer API token assembled from
# named permission groups; every group entry is `derived` (static analysis
# of the module sources, cited in notes) until the live capture lane
# upgrades it to `proven`. Schema: iac/componentpermissions/v1.
apiVersion: iac.planton.dev/v1
kind: ComponentPermissions
metadata:
  name: cloudflarenotificationwebhook
spec:
  cloudflare:
    groups:
      - purpose: ManageWebhook
        name: Notifications Write
        scope: com.cloudflare.api.account
        provenance: derived
        notes: Covers the Terraform module's single cloudflare_notification_policy_webhooks resource and the Pulumi module's matching NotificationPolicyWebhooks resource -- create/update/delete the webhook destination via POST/PUT/DELETE /accounts/{account_id}/alerting/v3/destinations/webhooks[/{id}] (derived from the module's API surface; neither engine performs secondary lookups). Group names verbatim from the Cloudflare API token permissions reference (https://developers.cloudflare.com/fundamentals/api/reference/permissions/, retrieved 2026-08-22).
      - purpose: ReadWebhook
        name: Notifications Read
        scope: com.cloudflare.api.account
        provenance: derived
        notes: Read-side of the same resources -- refresh/import the webhook via GET /accounts/{account_id}/alerting/v3/destinations/webhooks/{id} (derived from the module's API surface).
