# Cost profile: what drives this component's bill. Field paths are
# spec-relative proto field names, held to the served contract by the
# catalog-data conformance gate. Schema: finops/componentcostprofile/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostProfile
metadata:
  name: cloudflarezerotrustdnslocation
spec:
  billing_model: free
  notes: >-
    DNS locations create no charge of their own -- Gateway DNS filtering is
    included in every Zero Trust plan (Free covers 50 users). Seat billing
    is driven by enrolled users, never by the number of locations.
