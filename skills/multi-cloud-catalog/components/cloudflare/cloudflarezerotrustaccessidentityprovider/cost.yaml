# Cost profile: what drives this component's bill. Field paths are
# spec-relative proto field names, held to the served contract by the
# catalog-data conformance gate. Schema: finops/componentcostprofile/v1.
apiVersion: finops.planton.dev/v1
kind: ComponentCostProfile
metadata:
  name: cloudflarezerotrustaccessidentityprovider
spec:
  billing_model: free
  notes: Access identity providers are included in Zero Trust Free (50 users). Connecting an IdP creates no charge of its own. Seat counts and paid Zero Trust add-ons bill on the organization, never through this component.
