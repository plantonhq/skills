# Control profile: this component's posture against every control in the
# central catalog (catalog/_compliance/controls-catalog.yaml). Every control
# is examined; not_applicable states why. Evidence is verifiable against the
# spec contract and the official modules. Schema:
# compliance/componentcontrolprofile/v1.
apiVersion: compliance.planton.dev/v1
kind: ComponentControlProfile
metadata:
  name: openfgarelationshiptuple
spec:
  controls:
    - control_id: enc-at-rest
      status: not_applicable
      notes: "A tuple stores no customer data of its own beyond the grant it expresses -- it is a row persisted inside the OpenFGA server's backing datastore (Postgres, MySQL, or SQLite); encryption at rest is that datastore deployment's posture, not a choice any of this spec's fields can make."
    - control_id: enc-at-rest-cmk
      status: not_applicable
      notes: There is no data at rest for this component to key (see enc-at-rest); key management belongs to the datastore behind the customer-run OpenFGA server.
    - control_id: enc-in-transit
      status: not_applicable
      notes: "The tuple terminates no connections. The API calls that write and read it travel to the OpenFGA server, whose TLS posture is server deployment configuration -- outside this spec."
    - control_id: no-public-access
      status: not_applicable
      notes: A tuple has no network endpoint to expose; it is addressed only through the OpenFGA server's API, whose exposure is the server deployment's concern.
    - control_id: restricted-ingress
      status: not_applicable
      notes: No network surface exists to filter; the tuple is a logical object inside the OpenFGA server.
    - control_id: edge-protection
      status: not_applicable
      notes: A tuple receives no HTTP requests directly -- request filtering would attach to the OpenFGA server deployment or the proxy in front of it, neither of which this spec configures.
    - control_id: rbac
      status: configurable
      field_path: user
      evidence:
        type: spec_default
        reference: "The tuple IS one access-control grant -- the required user, relation, and object fields assert that a subject holds a relation on a resource, and the OpenFGA server evaluates permission checks from exactly these rows. The user message composes direct subjects (user:anne), usersets (group:engineering#member via the optional relation field), and wildcards (id \"*\" grants every subject of the type); the optional condition gates the grant on runtime context evaluated at check time."
      notes: "Every tuple field is mandatory except the model pin and condition, but the grant's breadth is the operator's -- a wildcard user or an over-broad relation grants exactly what it says, so the control is configurable rather than enforced. The shipped presets (01-user-document-access, 02-group-membership) compose direct and group-inherited grants. Who may WRITE tuples to the server is the OpenFGA server's authentication posture (see iac/permissions.yaml), not a spec field."
    - control_id: workload-identity
      status: not_applicable
      notes: "The tuple holds no credentials and authenticates to nothing. The credential the IaC runner presents to the OpenFGA server is the provider connection's concern, documented in iac/permissions.yaml -- no spec field selects between static and federated auth."
    - control_id: no-legacy-auth
      status: not_applicable
      notes: "A tuple carries no credential path of its own to weaken or disable. The OpenFGA server's authentication method (none, pre-shared key, or OIDC) is server deployment configuration outside this spec."
    - control_id: strong-user-auth
      status: not_applicable
      notes: The component itself enforces no end-user sign-in -- it packages no human sign-in surface; any credentials it carries are workload or administrative access, whose posture rbac and workload-identity carry.
    - control_id: audit-logging
      status: not_applicable
      notes: "The spec exposes no audit surface. The OpenFGA server emits structured logs as server-level posture; there is no per-tuple audit toggle in the OpenFGA API or the modules."
    - control_id: monitoring-integration
      status: not_applicable
      notes: OpenFGA publishes Prometheus metrics and optional OTLP traces at the server level; no per-tuple metric surface exists in the API or this spec.
    - control_id: backup-recovery
      status: not_applicable
      notes: "The tuple is declarative configuration fully re-creatable from the manifest, and its persisted row lives in the OpenFGA server's backing datastore, whose backups are that database deployment's posture. No per-tuple backup surface exists."
    - control_id: backup-retention-policy
      status: not_applicable
      notes: No backups exist at this component's level to retain (see backup-recovery); retention is the backing datastore's policy.
    - control_id: multi-zone-ha
      status: not_applicable
      notes: A tuple has no placement to configure. Availability is the OpenFGA server deployment's posture (server replicas plus datastore replication), none of it reachable from this spec.
    - control_id: deletion-protection
      status: not_applicable
      notes: "Neither the OpenFGA API nor the Terraform provider exposes a deletion-protection flag for tuples: a delete request proceeds when issued, and because every field is immutable, any field change itself deletes the old tuple and writes a new one. Protection, where wanted, is an operational concern outside this spec."
    - control_id: managed-patching
      status: not_applicable
      notes: A tuple is a pure configuration object with no software of its own to patch; upgrading the OpenFGA server binary is the server deployment's channel.
