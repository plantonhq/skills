# Least-privilege permissions the IaC runner's Kubernetes principal needs to
# provision, update, and destroy this component with the official modules.
# Every entry is `derived` (static analysis of the module sources, cited in
# notes) until the live capture lane upgrades it to `proven`. Schema:
# iac/componentpermissions/v1.
apiVersion: iac.planton.dev/v1
kind: ComponentPermissions
metadata:
  name: kubernetesgateway
spec:
  kubernetes:
    rules:
      - api_groups:
          - gateway.networking.k8s.io
        resources:
          - gateways
        verbs:
          - get
          - create
          - update
          - patch
          - delete
        cluster_scoped: false
        provenance: derived
        notes: Covers the kubectl_manifest.gateway resource -- the module's only resource, applying one namespaced Gateway (gateway.networking.k8s.io/v1) via server-side apply. The load balancer and proxies are provisioned by the GatewayClass's controller, not by the runner.
