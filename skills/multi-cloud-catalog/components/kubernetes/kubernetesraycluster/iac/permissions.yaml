# Least-privilege permissions the IaC runner's Kubernetes principal needs to
# provision, update, and destroy this component with the official modules.
# Every entry is `derived` (static analysis of the module sources, cited in
# notes) until the live capture lane upgrades it to `proven`. Schema:
# iac/componentpermissions/v1.
apiVersion: iac.planton.dev/v1
kind: ComponentPermissions
metadata:
  name: kubernetesraycluster
spec:
  kubernetes:
    rules:
      - api_groups:
          - ""
        resources:
          - namespaces
        verbs:
          - get
          - create
          - update
          - patch
          - delete
        cluster_scoped: true
        provenance: derived
        notes: Covers kubernetes_namespace_v1.namespace -- created and owned only when spec.create_namespace is true; get is needed for refresh either way. delete applies only to module-owned namespaces.
      - api_groups:
          - ray.io
        resources:
          - rayclusters
        verbs:
          - get
          - create
          - update
          - patch
          - delete
        cluster_scoped: false
        provenance: derived
        notes: Covers kubectl_manifest.ray_cluster -- the RayCluster (ray.io/v1) applied via server-side apply, the module's ONLY custom resource. The head pod, worker pods, `<name>-head-svc` Service, and the generated bearer-token Secret are created by the KubeRay operator reconciling this CR under the operator's own RBAC (its finalizer also owns the delete cascade -- the module deletes with Background propagation) -- the runner touches only the CR.
